# AI Usage Log

Record material Codex and Claude work here. Append entries; do not rewrite prior
accepted history. Missing evidence is written as “not run” or “pending,” never
inferred.

| Date (UTC) | Tool / agent | Task | Branch | Changed files or areas | Tests | Human review | Acceptance | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-25 | Codex | WP-00/WP-01 repository bootstrap | `chore/repo-foundation` | Initial repository foundation | Pending | Pending | In progress | No commit or push authorized |
| 2026-07-25 | Codex | WP-00/WP-01 implementation and local verification | `chore/repo-foundation` | Governance, ADRs, package, configs, contracts, CLI, tests, CI, notebook | Ruff lint/format, mypy, 28 pytest tests, doctor, smoke, notebook JSON passed | Pending | Ready for review | Repo remains unborn; nothing staged, committed, or pushed |
| 2026-07-25 | Codex | Pre-commit Python CI matrix correction | `chore/repo-foundation` | `.github/workflows/ci.yml`, project state, handoff, AI usage log | Local Python 3.11.9: Ruff lint/format, mypy, and 28 pytest tests passed | Pending | Ready for review | CI now targets Python 3.10 and 3.11; remote jobs not yet run; nothing staged, committed, or pushed |
| 2026-07-25 | Human project owner | Independent WP-00/WP-01 verification | `chore/repo-foundation` | Full repository foundation | Python 3.11.9 `.venv`; Ruff check passed; Ruff format check passed; mypy passed; pytest 28/28 passed; doctor passed; normal smoke passed; deterministic smoke byte-level comparison passed; secret scan clean | Independently performed by human | G0 and G1-LOCAL approved; G1-CI pending | Scientific decisions and gate approval are human responsibilities |
| 2026-07-25 | Codex | Pre-commit documentation cleanup and staging preparation | `chore/repo-foundation` | Project state, handoff, AI usage log | Python 3.11.9: Ruff check/format, mypy, pytest 28/28, and requested cache ignore checks passed | Pending staged-diff review | Ready for review | No code, architecture, or dependency change; no commit or push |
| 2026-07-26 | Codex | Record Stage 1 approvals and prepare isolated PIDNet-S Stage 2 execution | `feat/first-vertical-slice` | Evidence/state docs; Pillow dependency; single-image loader/manifest; PIDNet config/runner; native/aligned metadata; MSP/entropy; Colab notebook; tests | Ruff check passed; Ruff format passed; mypy passed for 22 source files; pytest 96/96 passed; `git diff --check` passed | PIDNet commit, dataset roles, sealed SMIYC boundary, and restricted checkpoint usage approved by human; real Colab forward and visual review pending | Local preparation complete; execution blocked on human inputs/runtime | No upstream checkout, checkpoint, dataset, legal image, logits, metric, or ONNX artifact created; no stage, commit, or push |
| 2026-07-26 | Codex | Apply approved upstream sample decision and probe official checkpoint access | `feat/first-vertical-slice` | PIDNet spike config/runner/notebook, upstream-sample provenance/tests, evidence/state/handoff/license records | Ruff check/format passed; mypy passed for 22 source files; pytest 98/98 passed; `git diff --check` passed | Human approved the two fixed-checkout sample candidates and their plumbing-only boundary; real checkpoint hash/load/forward and visual review pending | Sample gate complete; checkpoint/Colab gate pending | Fixed checkout HEAD/origin/clean state and both sample hashes/shapes verified; direct checkpoint link failed, replacement folder reachable, no checkpoint or model output created; no stage, commit, or push |
| 2026-07-26 | Codex | Pin human-verified PIDNet-S checkpoint identity without loading it | `feat/first-vertical-slice` | PIDNet config schema/YAML, runner pin source, Colab filename/hash guard, tests, evidence/state/handoff | Ruff check/format passed; mypy passed for 22 source files; pytest 98/98 passed; `git diff --check` passed | Filename, official replacement-folder source, and SHA-256 supplied by human; real load/forward and visual review pending | Config/notebook pin complete; Colab gate pending | Absolute local path intentionally omitted; checkpoint not opened or copied; `weights_only=True` preserved; no stage, commit, or push |
| 2026-07-26 | Codex | Harden PIDNet loader and Colab wrapper from first T4 development evidence | `feat/first-vertical-slice` | PIDNet loader/report, focused tests, Colab execution wrapper, Stage 2 state/task/handoff/evidence records | Ruff check passed; Ruff format check passed for 72 files; mypy passed for 22 source files; pytest 113/113 passed; notebook JSON and structural checks passed; `git diff --check` passed | Human supplied the real checkpoint layout and T4 run evidence; clean rerun and visual sanity remain human gates | Hardened diff ready for human review; clean reproducibility artifact pending | T4/CUDA feasibility run produced native `[1,19,64,128]` and aligned `[1,19,512,1024]` logits with byte-identical repeats, but was Git-dirty due to a temporary loader patch. No stage, commit, push, Cityscapes, OOD, ONNX, TensorRT, or deployment work. |
| 2026-07-26 | Codex | Close bounded Stage 2 Block 0 for Commit A review | `feat/first-vertical-slice` | Loader/notebook review; real origin pin; path-free spike command metadata; focused tests; required state/evidence records | Ruff check passed; Ruff format check passed for 72 files; mypy passed for 22 source files; pytest 116/116 passed; notebook/path/secret and artifact/binary scans passed; `git diff --check` passed | Commit A and the following local PyTorch/checkpoint block require human approval | Ready for Commit A diff review | No local PyTorch install, checkpoint read, Cityscapes extraction, dependency/license change, stage, commit, or push. |
| 2026-07-26 | Codex | Build local checkpoint and Cityscapes val foundation | `feat/first-vertical-slice` | Environment-only PyTorch inventory; strict real-checkpoint CLI/test; CPU/MPS runner metadata; val-only extraction/manifest; Cityscapes adapter/LUT; state/handoff | Ruff check passed; Ruff format check passed for 79 files; mypy passed for 23 source files; pytest 131 passed with 2 expected opt-in skips; real checkpoint and Cityscapes opt-in tests 2/2 passed; `git diff --check` passed | Commit A approved through continuation; Commit B diff and following metrics/runner block require human review | Ready for Commit B review | PyTorch 2.13.0 remained environment-only. CPU/MPS forwards were finite and repeatable within backend. Cityscapes val manifest is 500/500/3 with hash `7e91ab…f852`. No stage, Commit B, push, Fishyscapes, ONNX, SMIYC, or license change. |
| 2026-07-26 | Codex | Build Commit C candidate and complete the mandatory local 1/5/10-image campaign | `feat/first-vertical-slice` | Semantic metrics; MSP/entropy/MaxLogit/Energy; minimal path-free evaluator; local/full-resolution configs; deterministic selection and small visuals; artifact packager; thin Colab notebook; tests and state/handoff | Ruff check passed; Ruff format check passed for 87 files; mypy passed for 25 source files; pytest 155 passed with 2 expected opt-in skips; notebook JSON and six code cells compiled; path/secret and large-file scans clean; `git diff --check` passed | Commit B approved through continuation; Commit C diff, commit, push, exact Colab commit, and scientific interpretation remain human gates | Ready for Commit C review | Local 1 CPU, 5 MPS, and 10 MPS runs completed without failures. Development-grid mIoU was approximately 0.6632/0.6121/0.6246 and pixel accuracy 0.9637/0.9514/0.9565; these are not official-protocol or final results. All four ID-only score summaries were finite. External 10-image ZIP SHA-256 is `40c0033…1d408`. No stage, Commit C, push, SMIYC, Fishyscapes, ONNX, or threshold work. |
| 2026-07-26 | Human project owner / Codex | Approve and create the local Commit C | `feat/first-vertical-slice` | Reviewed Commit C candidate: metrics, scores, minimal evaluator, local campaign support, packager, Colab preparation, tests, and handoff records | Pre-commit gate: Ruff check/format, mypy, pytest 155 passed with 2 expected opt-in skips, notebook compile, path/secret/large-file scans, and `git diff --check` passed | Human explicitly approved continuation and the local Commit C; push remains a separate human gate | Approved for local commit | No push, Colab execution, artifact promotion, SMIYC, Fishyscapes, ONNX, or threshold work authorized. |
| 2026-07-26 | Human project owner / Codex | Verify full Cityscapes evidence and add the public Fishyscapes development foundation | `feat/first-vertical-slice` | Independent external ZIP verification; Cityscapes evidence record; truthful selection strategies and cross-city visuals; manual-only Lost & Found adapter/manifest; NumPy AP/FPR95; Fishyscapes role/manual plan; state/handoff | ZIP SHA/size/CRC, 42 internal hashes, manifest hashes, pixel totals, clean exact commit, finite metrics/scores, path/secret checks passed; Ruff check/format passed for 93 files; mypy passed for 27 source files; pytest 174 passed with 2 expected opt-in skips; one-image CPU smoke 1/1; `git diff --check` passed | Human supplied the completed campaign evidence and authorized this implementation block; new diff/commit remains pending human review | Ready for review | Full run was 500/500 with zero failures at clean Commit C. Runtime is end-to-end evaluation-pipeline timing, not pure inference or Jetson FPS. This is the project single-scale evaluation, not guaranteed official-protocol reproduction. No Colab rerun, Fishyscapes/SMIYC access, threshold, training, calibration, commit, or push. |
| 2026-07-26 | Codex | Fix Python 3.10 predictive-entropy typing compatibility | `feat/first-vertical-slice` | `src/edgeguard/scoring/uncertainty.py`, project state, handoff, and audit log | GitHub Actions failure independently inspected; isolated Python 3.10.11 with NumPy 2.2.6 and mypy 1.20.2 passed editable install, Ruff check/format, mypy, and pytest 174 with 2 expected skips; local Python 3.11.9 Ruff check/format, mypy, and pytest 174 with 2 expected skips also passed | Human required the compatibility correction before accepting EG-OOD-001 | Ready for human review; remote CI pending until push | `predictive_entropy` now materializes its reduction with `np.asarray(..., dtype=np.float32)` before validation. No NumPy pin/downgrade, API change, stage, commit, or push. |
| 2026-07-26 | Codex | EG-THESIS-001 scope, title, claim, and task-system migration | `feat/first-vertical-slice` | Charter and README; five coherent master architecture/data/experiment/claim documents; work packages, protocol, decisions, tasks, research role records, state, and handoff | Ruff check passed; Ruff format check passed for 98 files; mypy passed for 27 source files; pytest passed 174 with 2 expected skips; `git diff --check` passed; 3 notebooks parsed and 3 focused notebook tests passed; changed-file path/secret/large/binary/artifact scans clean | Human accepted the complete 17-file documentation block for one coherent local commit and authorized push to the feature branch; university title approval remains external | Accepted for commit; remote CI pending | Started from clean Commit D `345d9fd1dcff0a7aa9c54c6f3929c2c751c24c7c`; preserved measured PIDNet/Cityscapes/OOD foundation facts; no acquisition, framework install, training, Fishyscapes inference, SMIYC access, or Jetson action. |
| 2026-07-27 | Codex | EG-DATA-001 storage, access, ontology, and split-analysis gate | `feat/first-vertical-slice` | Runtime external-root name; compact four-namespace ontology YAML and validator/tests; storage lifecycle; dataset access matrix; Cityscapes split-analysis protocol; decisions/tasks/state/handoff | Ruff check passed; Ruff format check passed for 100 files; mypy passed for 28 source files; pytest passed 183 with 2 expected skips; ontology tests 9/9; `git diff --check` and notebook/path/secret/large/binary/artifact scans passed | Drive root, provisional ontology, dataset access/terms, first acquisition, and all listed later gates require human review | Locally implemented and tested; pending human acceptance | Root corrected to `EdgeGuard/` with `private_inputs/` as legacy/current child; official val corrected to non-sealed `official_val_common_eval`; ontology payload SHA-256 `24fd17b…0a10`; no data acquisition/extraction, Drive mutation, framework install, training, SMIYC/Jetson access, stage, commit, or push. |
| 2026-07-27 | Codex | EG-DATA-002 Cityscapes Fine train preparation | `feat/first-vertical-slice` | Train-only archive preparation; deterministic train-ID masks; streaming class/group analysis; three split candidates; evidence bundle; thin Colab notebook; tests and handoff | Ruff check passed; Ruff format check passed for 103 files; mypy passed for 29 source files; pytest passed 201 with 2 expected skips; `git diff --check` passed | Human diff review, exact commit, real Colab archive verification/preparation, evidence review, and split selection pending | Locally tested only | Private Drive was unavailable locally; synthetic ZIP fixtures only. Preparation time stays in the operational receipt and is excluded from deterministic dataset/split identities. No real archive/data/Drive mutation, split freeze, training framework, training, SMIYC/Jetson access, stage, commit, or push. |
| 2026-07-27 | Codex | EG-SEG-001 pinned semantic training laboratory | `feat/first-vertical-slice` | Exact MMSeg proposal; common/five-model configs; accepted-split handoff; experiment/checkpoint/resume/registry/native-logit contracts; runtime install/train/probe/verifier commands; thin Colab notebook; tests and handoff | Ruff check passed; Ruff format check passed for 117 files; mypy passed for 36 source files; pytest passed 223 with 2 expected skips; 26 focused tests and `git diff --check` passed | Human diff review, commit, resolved Colab stack evidence, framework/license acceptance, real EG-DATA-002 evidence, split selection, and pretrained source decisions pending | Repository implementation locally tested only | MMSeg/Torch/CUDA were not installed or imported locally; no GPU probe, Drive access, real data processing, scientific training/result, official-val selection, Fishyscapes/SMIYC, Jetson, stage, commit, or push. |
| 2026-07-27 | Human project owner / Codex | Accept EG-SEG-001 and add the bounded Cityscapes diversity split policy | `feat/first-vertical-slice` | Existing semantic laboratory; manifest-only D/E split rebuild; hard diversity constraints and automatic policy selection; cryptographic training handoff; validation-interval metric record; focused tests and minimal state/handoff updates | Focused tests 39/39; synthetic 1,890-group rebuild 15.298 seconds; Ruff check passed; Ruff format check passed for 120 files; mypy passed for 37 source files; pytest passed 227 with 2 expected skips; `git diff --check` passed | Human accepted EG-SEG-001 and authorized autonomous bounded correction, commit, push, and CI inspection; framework/pretrained-source approval and real training remain gated | Executable scientific milestone ready for commit | Human-supplied 2,975-sample evidence rejects A/B/C for city/sequence domination. No images or masks were reprocessed; no official val, Fishyscapes, SMIYC, framework install, GPU probe, or real training occurred. |
| 2026-07-27 | Codex | EG-SEG-002 current-Colab compatibility, persistent acquisition, staging and five-model smoke automation | `feat/first-vertical-slice` | OpenMIM-free two-path installer; live long-run telemetry and recovery; Drive-aware Cityscapes staging; approved acquisition queue and CPU notebook; deterministic fractions; bounded five-model smoke notebook/runner; focused tests and minimal status records | Ruff check passed; Ruff format check passed for 132 files; mypy passed for 39 source files; pytest passed 242 with 2 expected opt-in skips; notebook JSON and 31 Python code cells validated; config/ontology and changed-file path/secret/large/binary/artifact scans passed; `git diff --check` passed | Human authorized autonomous implementation, one coherent commit and push; real Colab execution remains external | Repository implementation locally tested; remote CI pending | No local framework install, Drive access, data extraction, real smoke training, official val, Fishyscapes, Lost & Found, SMIYC or Jetson access. Compatibility selection and scientific measurements remain pending real Colab execution. |
| 2026-07-27 | Codex | Establish strict local-first Colab readiness gate | `feat/first-vertical-slice` | Explicit runtime contract; terminal bootstrap evidence; project-owned mini-Cityscapes fixture; 11-phase readiness command; five-model CPU probe; storage inventory/dry-run; split compatibility and smoke notebooks; manual Linux CPU workflow | Focused tests passed; local Apple M1/Python 3.11.9 five-model CPU compatibility probe passed; Ruff check passed; Ruff format check passed for 141 files; mypy passed for 40 source files; pytest passed 260 with 2 expected skips; `git diff --check` passed | Human authorized autonomous implementation, one commit, feature-branch push and remote gate inspection | Local readiness passed; remote normal CI and manual Linux CPU gate pending | No real dataset, pretrained weight, training, Drive mutation, scientific metric or performance claim. Colab remains blocked until both remote gates pass. |
| 2026-07-27 | Codex | Activate feature-branch Linux x86 semantic CPU gate | `feat/first-vertical-slice` | Restricted feature-branch push trigger and workflow contract test | Operational commit `ee2a292`; normal CI Python 3.10/3.11 passed in run `30275255120`; Linux x86 Python 3.12 five-model CPU probe and evidence upload passed in run `30275255124` | Human authorized the operational commit/push and iterative remote verification | Completed and remotely verified | CPU-only Torch, MMEngine 0.10.7, mmcv-lite 2.1.0 and pinned MMSeg were used; no dataset, CUDA or full MMCV. |
| 2026-07-27 | Codex | Establish local OOD scoring and semantic calibration core | `feat/first-vertical-slice` | Backend-neutral uncertainty scores; deterministic log-temperature fitting; NLL/ECE/Brier/reliability metrics; synthetic fixture evidence command; tests and handoff | Focused tests 22/22; local readiness 11/11 with Mac CPU models 5/5 and checkpoint resumes 5/5; Ruff check passed; Ruff format check passed for 145 files; mypy passed for 41 source files; pytest passed 273 with 2 expected skips; `git diff --check` passed | Human authorized bounded local implementation, scientific commit and feature-branch push | Locally tested; commit and remote verification pending | Synthetic fixture only. No real data/checkpoint, training, Drive mutation, calibrated anomaly-probability claim, context, temporal or fusion work. |
| 2026-07-27 | Codex | Complete autonomous local-first campaign and notebook handoff | `feat/first-vertical-slice` | Resumable campaign DAG; local-mini perception, OOD, calibration, anomaly-head, detection, context and temporal paths; export probe; assistant/thesis reporting; five thin notebooks; failure injection; state and handoff | Clean campaign 19/19; idempotent reuse 19/19; interruption resume attempt 2; notebook harness 5/5; Mac MMSeg models 5/5; local ONNX/ORT surrogates 5/5; focused and full local gates passed before handoff; final remote gates pending | User explicitly authorized broad autonomous local development and up to four coherent commits; scientific/data/Jetson gates remain human-owned | Locally complete; final documentation commit and remote verification pending | All new outputs are synthetic non-scientific pipeline evidence. No real data/weights, Drive, Colab, sealed data, TensorRT, Jetson, model ranking or threshold promotion. |
| 2026-07-27 | Codex | Execute final comprehensive local closure | `feat/first-vertical-slice` | Project audit and dataset catalog; data quality/transforms/loaders/acquisition; five actual MMSeg mini trainings and ONNX probes; YOLO11n/RT-DETR-R18 mini training/export; feature anomaly heads; OOD/calibration/statistics; mini HPO; full video/Streamlit; lineage; preregistration/deployment contracts; notebook UI; four evidence packs | Focused tests 43/43; full Python 3.11 gate Ruff/format/mypy passed and pytest 328 passed with 2 optional skips; `EG-LOCAL-COMPLETE` completed all stages and idempotent rerun reused all; actual semantic/detector/HPO/temporal interruption-resume passed; normal and Linux remote gates pending | User authorized autonomous local closure, at most five coherent commits, push and CI inspection; all scientific/data/sealed/Jetson choices remain human-owned | Local engineering closure complete; remote verification pending | No real dataset, pretrained weight, Drive, Colab, sealed data, CUDA performance, TensorRT engine, Jetson measurement, scientific ranking or promotion. All fixture results are non-scientific pipeline validation. |
| 2026-07-27 | Codex | Final bounded pre-Colab stabilization | `feat/first-vertical-slice` | PIDNet-S and RT-DETR-R18 layer-aware ONNX diagnostics; deterministic deployment package and verifier; canonical notebook boundary; exact-commit readiness command; external evidence packs and runbook; focused state/handoff updates | Mac focused package/notebook/readiness tests 19/19; Python 3.11 Ruff/format/mypy passed and pytest 339 passed with 2 optional skips; Mac/Linux final closure and remote CI still pending at record time | User authorized at most two coherent commits and remote verification; real-data, CUDA, Jetson, calibration, threshold, promotion, and thesis decisions remain external/human gates | First ONNX-classification commit created; final deployment commit and remote verification pending | Both random-weight architectures are classified `bounded_documented_drift` without tolerance loosening. Deployment package uses non-scientific placeholders and stays outside Git. No real data/weights, Drive, Colab, CUDA, TensorRT, Jetson, or scientific result. |
| 2026-07-28 | Codex | Implement human-approved semantic-first rescue path | `rescue/semantic-first` | Audit/split, three-model standard MMSeg path, evaluation/calibration/ACDC, ONNX/prediction, Streamlit, Jetson review script, Colab notebook, reporting, ADR/runbook/state | Ruff format/lint passed; mypy passed for 91 source files; pytest 344 passed with 10 skips after rescue dependencies; five CLI helps and Streamlit headless startup passed; pinned MMSeg 1.2.2 configs resolved 3/3 | Working tree review, commit/push, real split freeze, model promotion, final interpretation, and external runs pending | Implementation ready for review; no scientific result | No dataset/checkpoint download, Drive, official val, ACDC inference, CUDA training, ONNX artifact, TensorRT, Jetson, commit, push, or PR. |
| 2026-07-28 | Codex | Harden semantic-first data and stock-runner gates | `rescue/semantic-first` | Near-duplicate/black/all-ignore/crop-survival/co-occurrence/split-comparison audit; audit-receipt enforcement; train-fit/split-bound weights; common model-preprocessor size; MMSeg/ACDC path contracts; fail-closed mmcv-lite guard; state/handoff/runbook | Ruff passed; mypy passed for 98 rescue/source files; pytest 350 passed with 10 skips; all three configs resolved to common 512x1024 input; synthetic-only Fast-SCNN stock Runner completed one optimizer step, validation and checkpoint writes | Real-data review, split freeze, commit/push, and scientific interpretation pending | Local engineering gate passed; no scientific result | Local search found repeated 180-sample synthetic fixtures only, not licensed Cityscapes Fine train. Synthetic integration checkpoints were temporary and deleted. No real data, CUDA, official val, ACDC inference, ONNX artifact, Jetson, commit, push, or PR. |
| 2026-07-28 | Codex | Implement human-approved multi-domain generalization expansion | `rescue/semantic-first` | Cityscapes19 ontology v2; BDD100K/IDD20K train and withheld-val adapters/audits; frozen manifests; duplicate gates; equal-domain sampler; five-model step protocol; bounded Optuna HPO; pretrained provenance; global calibration; source/ACDC/sealed external evaluation; WildDash label-ID packaging; ONNX/demo/notebook/reporting; append-only run ledger; ADR/runbook/state | Ruff format/lint passed for 248 files; mypy passed for 97 source files; pytest 363 passed with 10 skips; five CLI help smokes, notebook JSON/eight-cell compile, compileall, and `git diff --check` passed | Real-data audit/split review, model/config freeze, sealed release, commit/push, and scientific interpretation pending | Local implementation complete; no new scientific result | No licensed dataset/model download, Drive mutation, CUDA run, official validation, ACDC/sealed inference, submission, ONNX artifact, Jetson action, commit, push, or PR. Fixture-count manifests are explicitly rejected by training. |
| 2026-07-28 | Codex | Verify official semantic sources and implement bounded public-data catalog/probe | `rescue/semantic-first` | Nine-dataset semantic catalog; certificate-verified source probes; safe public sample downloader; official A2D2 55-color mapping proposal; merge and sampling protocol; dataset documentation/tests | Nine official landing pages reachable; official A2D2 five-file bundle size/SHA-256 verified and RGB pair inspected; Ruff format/lint 253 files; mypy 98 source files; pytest 369 passed/10 skipped; catalog/source probes and diff check passed | User explicitly approved autonomous dataset expansion, test downloads, processing, cataloging, commit and push | Engineering evidence complete; scientific use remains gated | Only the public A2D2 engineering sample was downloaded under ignored cache. No licensed source corpus, model training, validation/external inference, model metric, sealed access, submission, ONNX or Jetson action. |
| 2026-07-28 | Codex | Diagnose and close remote CI after source-catalog delivery | `rescue/semantic-first` | Optional MMSeg integration test gate; GitHub Actions checkout/setup-python Node 24 majors; state/handoff | Local full suite remained 369 passed/10 skipped with MMSeg installed; remote CI run 30332310919 passed Python 3.10 and 3.11 with the optional integration absent | User authorized autonomous completion, commit and push | Remote quality gate passed | Root cause was test-environment coupling, not dataset or model code. No scientific/runtime artifact changed. |
| 2026-07-28 | Codex | Prepare the Drive/Colab data boundary and revise delivery notebooks | `rescue/semantic-first` | Official access contract/runbook; archive hashing; prepared-tree bundles; safe 175+25 GiB staging; preflight/training notebooks; exact-commit snapshots; catalog access corrections | Ruff format/lint passed for 259 files; mypy passed for 99 source files; pytest 374 passed/10 skipped; both notebooks parsed and every code cell compiled; `git diff --check` passed | User authorized autonomous pre-Colab preparation and removal of data without a defensible API/manual route | Local implementation complete; real Drive run remains external | No licensed dataset, Drive mutation, Colab session, GPU training, scientific metric, ACDC/sealed access, submission, ONNX or Jetson action. Inaccessible Hugging Face road probe and third-party mirrors removed; MUSES/KITTI access facts corrected. |
| 2026-07-30 | Codex | Implement the updated phase-one road-perception and Jetson thesis roadmap | `rescue/semantic-first` | Safe source-archive preparation; revised Colab notebooks; road/corridor/region/attention outputs and metrics; four-score frame uncertainty and source-frozen shift evaluation; TensorRT/Jetson target tools; conditional RTMDet-Tiny gate; canonical scope/task/claim documents | Ruff format check passed for 275 files; Ruff lint passed; mypy passed for 104 source files; pytest 393 passed/10 skipped; notebook regeneration/code-cell compilation, compile-all, CLI help, active path/credential scan and `git diff --check` passed | User explicitly approved implementation and autonomous technical ownership; licenses, sealed tests, Git publication and privileged device operations remain human-owned | Locally implemented and tested; external execution pending | No local archive extraction, Drive/Colab/GPU training, official validation, ACDC/sealed evaluation, ONNX/TensorRT artifact, Jetson action, commit, push or PR. Kaggle BDD remains smoke-only and scientifically ineligible. |
| 2026-07-30 | Codex | Harden Colab runtime, Drive recovery/review outputs, and thesis data figures | `rescue/semantic-first` | Receipt-driven selected runtime; complete Colab extras; IDD canonical bundle masks; explicit Drive layout; selective campaign snapshots; downloadable review ZIP; measured-only dataset CSV/PNG/PDF figures; local all-cell notebook contract runner | Ruff lint and 284-file format checks passed; mypy passed for 107 source files; pytest 401 passed/10 skipped; both delivery notebooks executed all 16 code cells locally in external-action-free mode; compile-all, CLI help and diff checks passed | User requested autonomous pre-Colab stabilization; Git publication and licensed-data/Colab execution remain external | Local engineering implementation passed | Local notebook mode proves code-cell integration only. No licensed archive extraction, Drive mutation, Colab CUDA run, scientific plot/result, checkpoint, ONNX/TensorRT/Jetson artifact, commit, push, or PR. |
| 2026-07-30 | Codex | Reconcile delivery with the connected legacy/current Drive layout | `rescue/semantic-first` | Read-only Drive tree/evidence audit; pinned Cityscapes legacy-bundle adapter; additive BDD/IDD/quarantine/download layout; physically separated Kaggle/official BDD bundles; notebook discovery and reuse gate; audit document and tests | Ruff lint and 285-file format checks passed; mypy passed for 107 source files; pytest 404 passed/10 skipped; both notebooks executed all 16 cells locally; diff check passed | User questioned the assumed Drive structure before Git approval; no Drive mutation authorized or performed | Local compatibility implementation passed | Existing Cityscapes archives, prepared v1 tree, 6.99 GB bundle, real manifests, historical A100 compatibility evidence and EG-REAL-001 were observed. No file was moved, renamed, copied, deleted or uploaded. |
| 2026-07-30 | Codex | Bind delivery notebooks to completed `private_inputs` uploads and harden Colab recovery | `rescue/semantic-first` | Second read-only Drive inventory; generic private-input discovery; IDD/BDD preparation budgeting and idempotence; BDD provenance-limited scope; Cityscapes+IDD campaign identity; dependency/import probes; persisted runtime failure evidence; notebook/runbook/state/tests | Ruff lint and 285-file format checks passed; mypy passed for 107 source files; pytest 406 passed/10 skipped; both notebooks regenerated and all 16 code cells executed locally in external-action-free mode; compile-all and diff checks passed | User reported completed Drive uploads and requested Colab stabilization; Drive mutation and Git publication remain unauthorised | Local engineering implementation passed | Drive reports official Cityscapes, official IDD I/II and Kaggle BDD in `private_inputs`; local large archives were removed by the user. No Drive write, archive extraction, Colab CUDA run, scientific metric, commit or push occurred. |
| 2026-07-30 | Codex | Add persistent Colab failure evidence and close pre-publication gates | `rescue/semantic-first` | Bootstrap/full exception reporters; secret redaction; bounded diagnostic hashing/ZIP; IPython notebook hook; Drive failure layout; latest-report browser download; documentation and tests | Ruff lint/format passed for 288 files; mypy passed for 108 source files; pytest 411 passed/10 skipped; 16 notebook cells passed locally; notebook regeneration was hash-idempotent; compile-all, eight CLI helps and diff checks passed | User explicitly approved final hardening, commit and push | Ready for exact-commit pin and publication | Failure packages exclude licensed data, checkpoints, ONNX/TensorRT and environment variables. No Drive write, Colab CUDA run or scientific metric occurred during local validation. |
| 2026-07-30 | Codex | Pin both delivery notebooks to the verified implementation commit | `rescue/semantic-first` | Notebook generator, generated notebooks, pin assertions, state/handoff | Exact implementation commit `f25fc4faa6f6a55aae226aec9c269d8c5cf3102c`; Ruff/mypy passed; 23 focused tests passed; both notebook regenerations were hash-identical and all 16 cells passed locally | User approved commit/push after final gap audit | Ready for pin commit and push | Notebook execution checks out the immutable implementation commit even if the branch later moves. |
| 2026-07-30 | Codex | Correct branch-tip CI/immutable-notebook test boundaries | `rescue/semantic-first` | CI dependency extras; generated training notebook; local harness pin handling; state/handoff | Initial remote CI exposed two environment-bound failures; focused tests 32/32 and full local pytest 411 passed/10 skipped; Ruff format/lint and mypy passed | User authorized error recovery, commit and push | Local correction passed; remote rerun pending | Colab still requires exact commit `f25fc4f…`; only external-action-free local mode tolerates a later branch-tip checkout. CI now installs thesis-figure dependencies through `rescue` extras. |
| 2026-07-30 | Codex | Diagnose the first real Colab Drive inventory failure and harden every notebook subprocess | `rescue/semantic-first` | Read-only Drive failure report; non-fatal per-archive inventory hash errors; bounded-retry Drive copies; redacted live command logs and actionable failure tails; both generated notebooks; tests/runbooks/state | Failure report `20260730T101713…61f1dd15` confirmed ~210 GB free and an exit-code-only child failure during mounted-Drive hashing; Ruff format/lint and mypy passed; pytest 414 passed/10 skipped; notebook generation hash-idempotent | User supplied the real Colab failure and requested cross-notebook investigation | Local correction passed; exact-commit repin and remote CI pending | Scientific integrity remains fail-closed after copy; no archive was modified, extracted, or reclassified through Drive inspection. |
| 2026-07-30 | Codex | Repin both Colab delivery notebooks to the Drive-I/O-hardened implementation | `rescue/semantic-first` | Notebook generator, generated notebooks, pin assertions, state/handoff | Exact implementation commit `5fec521e4e495bccbc819198ca288ae47d96ed92`; focused pin/notebook tests pending this commit | User requested correction of the real Colab failure and cross-notebook risk review | Pin update prepared | The immutable checkout now contains the inventory, retry and subprocess-log corrections. |
| 2026-07-30 | Codex | Correct the exact config-resolution root cause revealed by the new Colab command log | `rescue/semantic-first` | Repository-root CLI defaults for preparation/audit/train/evaluate; repository-root cwd and explicit `PYTHONPATH` for every notebook child process; cwd-independence regression test; state/handoff | Real rerun showed `FileNotFoundError: configs/dataset/colab_data_access_v1.yaml` from `/content`; focused 39/39 tests, Ruff and mypy passed before full gate | User supplied the actionable command log | Local correction passed; repin and remote CI pending | The prior mounted-Drive-I/O hypothesis is superseded for this incident. The defensive hash/copy protections remain useful but were not the direct cause. |
| 2026-07-30 | Codex | Repin delivery to the repository-root config correction | `rescue/semantic-first` | Generator, both notebooks, pin tests, state/handoff | Exact implementation commit `6745106f0cb419dcc15b51cc3856eb87042086a6`; full local gate 415 passed/10 skipped before repin | User requested correction of the live Colab failure | Pin prepared for publication | Both notebooks now check out the implementation that fixes the observed relative-config failure and the same risk in later stages. |
| 2026-07-31 | Codex | Diagnose and remove the official IDD20K 12-hour preparation stall | `rescue/semantic-first` | One-pass archive hashing and streaming gzip extraction; LUT/bounded mask rendering; bundle/staging progress and write-time hashing; retry cache; subprocess-group cancellation; regenerated preflight notebook and tests | User-observed run remained silent for more than 12 hours after copying 25.8 GB; Ruff format/lint passed, mypy passed for 108 source files, pytest 419 passed/10 skipped, and both delivery notebooks executed all 16 cells locally | User supplied real Colab output and asked whether the duration was normal; prior authorization covered error recovery/publication | Local correction passed; exact-commit repin required before retry | The stalled run is rejected as operational evidence. No local licensed data, Drive write, scientific metric, GPU training, external evaluation, ONNX/TensorRT or Jetson action occurred. |
| 2026-07-31 | Codex | Repin delivery notebooks to the bounded IDD preparation implementation | `rescue/semantic-first` | Notebook generator, both generated notebooks, pin assertions, state and handoff | Exact implementation commit `3134d3f1e6d3bf23ede14f7a29b0adbeb51e0e89`; focused pin/notebook gates passed before publication | User previously authorized Colab error recovery commit and push | Pin update ready for publication | Colab will no longer execute the known-stalling `6745106` implementation. |
| 2026-07-31 | Codex | Implement session-loss-safe Cityscapes+IDD Colab campaign | `rescue/semantic-first` | IDD 500-sample shards; 250-sample audit catalog; content-addressed checkpoint generations; HPO rung/SQLite recovery; hash-bound completion receipts; dynamic effective-batch/runtime profiles; single-pass evaluation; two Run-all notebook surfaces | Exact implementation commit `3689880bc074befd326bc2831974f08f899ee42d`; Ruff format/lint and mypy passed; pytest 430 passed/10 skipped; both notebooks regenerated twice identically and all 17 code cells passed locally | User approved the resilience/performance plan and prior commit/push authority remains active | Locally verified engineering delivery; Colab CUDA/data execution remains external | No licensed data, Drive content, scientific metric, ONNX/TensorRT engine or Jetson measurement was produced locally. T4/L4/A100 smoke remains a real Colab gate. |
| 2026-07-31 | Codex | Correct the live Colab archive-copy receipt mismatch | `rescue/semantic-first` | Defensive receipt normalization; SHA-256 verification of an already-copied `/content` archive; stale `edgeguard` module purge after immutable checkout; generated notebooks and regression tests | User run copied the 11.8 GB Cityscapes archive but raised `KeyError: sha256`; exact correction commit `c69e0b356a083b2032a53674e9ce5267b08b3e2e`; 34 focused tests and all 17 local notebook cells passed | User supplied the live failure; existing error-recovery publication authority applies | Correction verified locally; Colab rerun required | The completed same-session `/content` copy can be adopted after local SHA-256 verification, avoiding a second Drive transfer. |
| 2026-07-31 | Codex | Correct official IDD20K Part I/II sample identity and exact release counts | `rescue/semantic-first` | Split/sequence/basename composite identity in preparation and audit; exact 14,027/2,036 train/validation gates; staging contract; dataset catalog and collision regression tests | Live official archives exposed repeated short ID `674060` in distinct sequences and exactly 16,063 labeled train/validation pairs; correction commit `3c04853107e4a01ecc83e8a8fcad331be6984b6b`; full pytest 432 passed/10 skipped and all 17 notebook cells passed | User supplied complete live extraction log | Correction verified locally; same-session archive cache remains reusable | Cityscapes preparation and its verified 8.26 GB Drive bundle completed successfully and must not be recomputed. IDD extraction must rerun from cached archives because the failed temporary extraction was correctly quarantined. |
| 2026-07-31 | Codex | Align IDD shard staging with the exact prepared release count | `rescue/semantic-first` | Shard-index validator and an exact 33-shard/16,063-sample regression fixture | Live preparation completed 33 shards and passed the scientific gate, while staging retained a stale 16,000 total assertion; correction commit `991cd55f213d49ae1723f96f7496a69e0f35b00b`; full pytest 433 passed/10 skipped | User supplied the first scientific-notebook staging traceback | Correction verified locally; staging rerun required | All prepared Drive shards remain valid; no archive copy, extraction, mask render or shard publication should repeat. |
| 2026-07-31 | Codex | Separate scientific data-quality decisions from infrastructure failures | `rescue/semantic-first` | Versioned source-defect quarantine, fail-closed ontology/integrity handling, persistent review packages, and notebook exit-code routing | Live IDD audit accounted for all 14,027 official train samples but found 9 invalid records; the old notebook surfaced the deliberate audit exit code 2 as an opaque subprocess crash | User supplied the complete audit log | Regression tests cover an accepted explainable exclusion and a rejected ontology violation | Exclusions are frozen before model selection, recorded in the dataset manifest, capped at 0.1%, and never permit mapping, leakage, duplicate, or integrity violations. |
| 2026-07-31 | Codex | Make the semantic runtime bootstrap compatible with current Colab uv releases | `rescue/semantic-first` | Bounded semantic-version validation accepting tested uv 0.x releases and recording the actual version | Colab supplied `uv 0.11.19 (x86_64-unknown-linux-gnu)` while the bootstrap required the exact legacy string `uv 0.8.8`, so installation stopped before CUDA/MMCV setup | User supplied the runtime compatibility traceback | Added a regression for the current platform-suffixed output; full pytest 436 passed/10 skipped | Versions below 0.8.8, 1.0 or newer, malformed output, and non-executable binaries remain fail-closed. |
| 2026-08-06 | Codex | Implement the approved Colab v2 stabilization and end-to-end pipeline | `stabilize/colab-v2` | Exact uv/Python/NumPy/Torch/MMCV/MMSeg hash locks; single runtime installer; three-model FP32/FP16 canary; generated thin training notebook; resumable 3→5 orchestrator; accepted Streamlit/thesis contracts; Jetson golden-vector handoff; tests and runbooks | Ruff format/lint passed for 304 files; mypy passed for 114 source files; pytest 452 passed/2 environment-gated skipped; notebook local execution and double-generation hash identity passed; CLI/compileall and `git diff --check` passed | User explicitly requested implementation of the supplied plan; Git publication and artifact promotion remain separate approvals | Local engineering implementation pending final human diff review and real Colab G1/G3 | No Drive mutation, licensed data processing, GPU training, model metric, accepted release, ONNX finalist, TensorRT engine, Jetson operation, commit, push, merge, or tag. |
| 2026-08-06 | Codex | Close Colab P0 lock, manifest-freeze and accepted-release gaps | `stabilize/colab-v2` | OpenMMLab two-wheel no-deps lock; GUI OpenCV lock rejection; canonical pipeline targets; human review receipts for training/official-val freezes; Cityscapes official-val manifest; final release candidate/promotion gate; accepted checkpoint restore; two-commit notebook generator | Main/OpenMMLab lock SHA-256 `ecf59a…865d` / `bf96b0…e924`; mypy passed for 115 source files; pytest 459 passed/2 skipped with only the intentionally unrepinned tracked-notebook assertion pending; temporary notebook generation compiled every code cell and was hash-identical twice | User approved implementation but explicitly reserved commit/push/tag/Drive/real-training actions for separate approval | Application implementation ready for review; immutable application commit and notebook delivery commit remain human gates | No tracked notebook repin, commit, push, merge, tag, Drive write, Colab GPU session, licensed-data processing, accepted scientific release, ONNX finalist, Jetson action or scientific claim was produced. |
| 2026-08-06 | Human project owner / Codex | Approve the immutable Colab v2 application commit and repin delivery notebooks | `stabilize/colab-v2` | Application commit `24b59a282e0bd7239ec3bd9edc179288dcca7460`; both generated delivery notebooks; publication-state records | Pytest 460 passed/2 environment-gated skipped; tracked preflight/training notebook SHA-256 `baab1b…5369` / `6bb5c0…4359`; double regeneration was byte-identical | User explicitly approved the application commit, notebook-delivery commit, and branch push | Local two-commit delivery gate complete; remote CI and real Colab G1/G3 remain external | No merge, tag, release, Drive write, Colab GPU session, licensed-data processing, accepted scientific release, ONNX finalist, Jetson action or scientific claim was produced. |
| 2026-08-06 | Codex | Correct Streamlit AppTest path portability revealed by branch CI | `stabilize/colab-v2` | Integration test and matching local-closure dashboard probe | Initial CI `31106694818` passed install/Ruff/format/mypy on Python 3.10/3.11; pytest failed only because new Streamlit resolves relative AppTest paths against the caller file. With a repository-root absolute path, the focused headless test and full local pytest 460 passed/2 skipped; Ruff/format/mypy/compile/diff gates passed. Replacement CI `31107068048` passed every step on Python 3.10/3.11. | Existing user approval covers the required CI correction and normal branch push | G0 branch CI green; real Colab G1/G3 remain external | This is a test/developer-probe portability correction; no Colab runtime lock, generated notebook, scientific artifact, merge, tag, Drive or Jetson state changed. |
| 2026-08-06 | Human project owner / Codex | Review the first real audit and bind cross-domain overlap provenance | `stabilize/colab-v2` | Hash-valid 31-file audit package; six-image cross-role near-duplicate review; candidate-source overlap audit and notebook generator | Cityscapes 2975/2975; IDD 14,027 accounted, 14,018 valid, nine policy-valid exclusions; three cross-role dHash pairs visually/numerically distinct; zero independently computed Cityscapes–IDD exact/near overlap. Application commit `0cf886443afe5be5c5c47c9598e3faae3f5ba896`; final pytest 460 passed/2 skipped after repin; Ruff/format/mypy/compile/diff passed. | User requested all required corrections so training can begin | Application correction committed and notebooks repinned; replacement remote CI and one bounded audit rerun required | No manifest freeze, GPU canary, training, metric, accepted release, merge, tag, TensorRT or Jetson action occurred. Persistent preparation bundles and audit catalogs remain reusable. |
| 2026-08-06 | Codex | Preserve and replace restored audit output whose immutable inputs changed | `stabilize/colab-v2` | Expected-input-aware completion quarantine and regenerated delivery notebooks | Real Colab failed before audit work because the caller rejected the legacy IDD receipt while the quarantine helper accepted it without the new source hash, leaving the output directory occupied. Application commit `bcfff06bae511da2646fc034291bb5cd33e28405`; pytest 460 passed/2 skipped; Ruff/format/mypy passed; notebook generation was byte-identical twice. | User supplied the exact Colab traceback and requested continued progress toward training | Correction and repin locally complete; branch publication and bounded audit retry required | The old audit remains recoverable under a timestamped quarantine directory; no dataset was rescanned, manifest frozen, or model trained by the failed run. |
| 2026-08-06 | Codex | Bootstrap the audited uv version independently of the Colab host image | `stabilize/colab-v2` | Private-prefix uv installation, host-mismatch recovery, exact private-binary validation, and updated readiness simulations | Real smoke stopped before runtime creation because Colab exposed `uv 0.11.19` while the installer rejected rather than replaced it. Application commit `5f665cdbe0caad011ff66ad7b210ab8121d9fad1`; an actual temporary prefix produced `uv 0.8.8`; pytest 460 passed/2 skipped; Ruff/format/mypy passed. | User supplied the complete runtime bootstrap traceback | Private pinned bootstrap and regenerated notebooks ready for publication and retry | No fallback matrix was selected, system uv was not modified, and no model step ran before the failure. |
| 2026-08-06 | Codex | Implement the single-notebook Colab v3 production campaign | `stabilize/colab-v2` | One generated master notebook; exact v2 audit reuse; five-model canary/screening/final; interruption and checkpoint recovery; top-two HPO; deterministic selection/ablations/policy acceptance; official-source evaluation; five-model Jetson, thesis and Streamlit packages; old notebook removal | Application commits `d35fb191fef509a6e8b8a09a38ccd24f97bb68ca` and formatting-only `2495354d02e45cc4f8748e94cfcf1862ae48a295`; Ruff and format passed for 245 files; mypy passed for 116 source files; pytest 454 passed/2 skipped; master notebook regenerated twice at identical SHA-256 `09f06e…d6f46` and all four cells passed in external-action-free mode | User explicitly authorized implementation, old notebook deletion, two commits and branch push | Local engineering delivery passed; real two-session L4 and interruption gates remain external | Read-only Drive inspection confirmed Cityscapes train/val bundle and 33 IDD shards. No local scientific metric, Colab GPU training, accepted real release, TensorRT engine, Jetson telemetry, merge or tag was produced. |
| 2026-08-06 | Codex | Remove hosted-Python bootstrap exposure and preserve the real Colab root error | `stabilize/colab-v2` | Standard-library-only private uv/Python bootstrap; hermetic-only restore/data/pipeline execution; v3 work-root isolation; incompatible-commit preservation; streamed stage/root-cause diagnostics; generated master notebook and regressions | Application commit `1da25ef405fcf36180d0b223973ff768296a228f`; Ruff and format passed for 301 files; mypy passed for 116 configured source modules; pytest 462 passed/2 skipped; both host entrypoints loaded with site packages disabled; notebook local contract passed | User supplied the real exit-code-only Colab failure and explicitly requested complete correction/publication | Local correction passed; immutable notebook repin and real L4 rerun remain | The prior notebook imported EdgeGuard/NumPy/Pydantic transitively and prepared data before installing the locked runtime; its failure ZIP discarded child stderr. No GPU training, scientific result, accepted release, Drive data mutation, TensorRT engine or Jetson measurement was produced by this correction. |
| 2026-08-06 | Codex | Correct the Colab system-pip private-prefix executable layout | `stabilize/colab-v2` | uv discovery across `bin` and `local/bin`; exact version probing in both bootstrap layers; verified executable PATH propagation; regressions | Real L4 output showed successful uv 0.8.8 installation followed by a false missing-file error at the assumed `bin/uv`; application commit `55e13db49f6becc640b3d3d7816ee3c888d4eeb7`; Ruff/format/mypy passed; pytest 466 passed/2 skipped | User supplied the complete actionable Colab log and requested immediate correction/publication | Local correction passed; immutable notebook repin and L4 continuation required | This was a Linux prefix-path defect, not a Torch/MMCV incompatibility. The already-installed Colab `local/bin/uv` can be reused on rerun. No model step, scientific metric, accepted release, TensorRT engine or Jetson measurement was produced. |
| 2026-08-07 | Codex | Harden the real L4 canary boundary after successful locked installation | `stabilize/colab-v2` | Torch/NVIDIA library-path precedence; isolated import/CUDA diagnostics; verified bootstrap-receipt reuse; Setuptools 80.9.0 compatibility lock; Linux exact-lock workflow | The supplied L4 log proved all 92 main packages, MMEngine/MMCV, MMSeg and EdgeGuard installed before the combined probe failed; exact-lock imports passed Linux workflow `31128389863`; pytest 470 passed/2 skipped; Ruff/format passed; mypy passed for 116 source modules and the three changed entrypoints; application commit `215742ee6bc9f94115701367389c9fc82698d876` | User supplied the complete log and previously approved aggressive correction/publication | Application correction committed/pushed; deterministic notebook repin prepared | No package fallback, invented scientific result, GPU training completion, accepted release, TensorRT engine or Jetson measurement is claimed. |
| 2026-08-07 | Codex | Make repeated Run-all canary attempts evidence-clean and lossless | `stabilize/colab-v2` | Failed runtime-evidence quarantine and master notebook repin | Focused runtime/master tests passed 50/50; final application commit `005eb035e515902cfd4cdd7c8a4426ae3fa52437`; notebook generated twice at identical SHA-256 `f904ee16…fd78d4b`; all four cells passed local claim-safe execution; remote run `31128538612` passed exact lock/imports, five-model CPU probe, real-codepath closure, ONNX classification and deployment-evidence build | User required a single retry-safe notebook and had supplied the preceding real failure | Final application and notebook delivery pushed; remote Linux closure green | Failed evidence is renamed, not deleted; work/checkpoint state is untouched. No scientific or deployment result is claimed. |
| 2026-08-07 | Codex | Isolate the locked runtime from Colab host state and close combined OOM/interruption recovery | `stabilize/colab-v2` | Hosted-environment firewall; Agg/cache isolation; headless PNG canary; environment identity; OOM-interruption resume helper; hostile-env workflow; master notebook repin | Real L4 log proved the exact 92-package install succeeded and failed only because hosted `MPLBACKEND=module://matplotlib_inline.backend_inline` leaked into the child. Application commit `3f3ef8f7d065f739650d75f7921fd8c7e748fe81`; Ruff/format/mypy passed; pytest 477 passed/2 skipped; notebook generated twice identically at SHA-256 `2d06b7af…a1e9a`; all four cells passed claim-safe local execution; hostile-context Linux workflow `31129018003` passed every exact-lock, five-model, ONNX and deployment-evidence step | User supplied the exact traceback and explicitly approved implementation, commits and branch push | Local correction, immutable notebook delivery and hostile Linux closure complete; real L4 continuation remains | Acceptance runs remain `not_run`; failed evidence and Drive state are preserved. No scientific metric, accepted release, TensorRT engine, Jetson measurement, merge or tag is claimed. |
| 2026-08-08 | Claude Code | Cross-file review: find and fix real training-blocking defects across data, model/training/HPO, and deployment layers | `stabilize/colab-v2` | `rescue/mmseg_runtime.py` (per-model pipeline extra steps, weighted-loss type coverage), `rescue/colab_data.py` (SIGALRM stall guard), `rescue/colab_pipeline.py` and `rescue/multidomain.py` (`verify_manifest_data_is_staged`), `models/semantic_local.py` (`semantic_environment_ready` mmcv-lite fix), new `tests/unit/test_mmseg_real_training_step.py`, `semantic-framework-cpu-probe.yml`, master notebook repin | Reproduced PIDNet-S's `AttributeError: 'SegDataSample' object has no attribute 'gt_edge_map'` against the pinned MMSeg checkout with the bug present, then confirmed the fix resolves it and all five architectures pass a real `model.loss()`+`backward()` step; confirmed the CE-vs-weighted-CE resolved config now differs for every model. Application commit `8ed151941bf36195fb77e354b174fbd253227c56`; Ruff/format/mypy passed; pytest 483 passed/17 skipped without the pinned stack, 498 passed/2 skipped with it; notebook generated twice identically at SHA-256 `85ce3c9a…963bac6` | User explicitly designated Claude Code as project lead for this review and approved implementation and commit | Local correction and notebook repin complete; remote hostile-context Linux workflow and real Colab L4 canary have not been re-run at this commit | No GPU training, scientific metric, accepted release, TensorRT engine, Jetson measurement, merge, push, or tag is claimed. The legacy-campaign candidate-hash trust chain (image bytes not re-hashed on stage) found during review is deliberately left for a separate, explicitly approved change since it touches dataset-identity policy. |
| 2026-08-08 | Claude Code | Faz 1: fix a mask train-ID validation bug and reconcile docs found stale by the same review | `stabilize/colab-v2` | `data/cityscapes.py` (`resize_train_ids` validation), `docs/SYSTEM_ARCHITECTURE.md`, `docs/DATA_CATALOG.md`, `docs/canonical-colab-runbook.md`, `docs/TASKS.md`, `configs/cityscapes_eval_{colab,local}.yaml`, `pyproject.toml` comment, master notebook repin | `resize_train_ids()` validated against the source label-ID space (0-33) instead of the train-ID space (0-18/255); added a regression using value 30 (valid source ID, invalid train ID). Application commit `1387322646f1f837ab2ed95200a4176ef0e3c4b4`; Ruff/format/mypy passed; pytest 484 passed/17 skipped without the pinned stack, 499 passed/2 skipped with it; notebook generated twice identically at SHA-256 `8e9943993a3c6df0292e77e82b32126990c4ddcad6a1a8ca7d1bd1e78c5abedc` | User explicitly asked to proceed to this phase after reviewing the prior commit | Local correction and notebook repin complete; remote hostile-context Linux workflow and real Colab L4 canary remain external | The IDD20K native-label-loss-on-shard-packaging finding from the review was, per explicit user decision, recorded as a known limitation in `docs/TASKS.md` rather than fixed, since correcting it requires re-processing already-staged Drive shards. No GPU training, scientific metric, accepted release, merge, push, or tag is claimed. |
| 2026-08-08 | Claude Code | Faz 2: tune data-loading parallelism for Colab High-RAM and add a Jetson video-demo script | `stabilize/colab-v2` | `configs/rescue/semantic_first.yaml` (`workers` 2→6), new `scripts/jetson/run_video_demo.py`, `edgeguard/rescue/inference.py` (`predict_tensorrt`, shared `_predict_with_runner`), `edgeguard/rescue/visualization.py` (`render_regions_overlay`), new `tests/unit/test_run_video_demo.py`, `docs/canonical-colab-runbook.md`, `docs/TASKS.md` (`DEMO-02`), master notebook repin | Confirmed `train_model`'s `precision="auto"` already selects bf16 on CUDA, so only `workers` needed tuning; `effective_batch`/HPO/step budgets in `PROJECT_CHARTER.md` are unchanged. `run_video_demo.py`'s ONNX/CPU path tested end to end with a synthetic fixture video (fixed `InferenceResult`, no real model needed); TensorRT path is untestable here and stays human-gated per `scripts/jetson/AGENTS.md`. Application commit `42be8d65de32c07b7857d966011254a213f7fed4`; Ruff/format/mypy passed; pytest 491 passed/17 skipped without the pinned stack, 506 passed/2 skipped with it; notebook generated twice identically at SHA-256 `9261c6e136fdec4187ccdc9c857fff6f3e339d8d5e56076e2936bf548ecfba91` | User explicitly requested resource-utilization tuning and the video demo script, and approved researching (not yet downloading) demo-video candidates | Local correction and notebook repin complete; remote hostile-context Linux workflow, a real L4 canary, and any real Jetson TensorRT run remain external | Demo-video candidates (Cityscapes' own official `leftImg8bit_demoVideo.zip`, the IDD portal, and CC0 stock dashcam footage) were researched and presented to the user but nothing was downloaded; no GPU training, scientific metric, accepted release, merge, push, or tag is claimed. |
| 2026-08-08 | Claude Code | Diagnose and fix a real L4 canary failure (AMP stack-probe hardcoded fp16) | `stabilize/colab-v2` | `scripts/train/train_semantic.py` (`_probe_model` AMP dtype selection), `src/edgeguard/rescue/mmseg_runtime.py` (new `resolve_auto_precision`, `train_model` refactored to call it), `tests/unit/test_semantic_first_rescue.py` (`resolve_auto_precision` policy tests), master notebook repin | User pushed commit `42be8d6` and ran the notebook on a real Colab L4; bootstrap and the full 92-package hermetic install succeeded, then `five-model-runtime-canary` failed on PIDNet-S: "AMP/FP16 stack-probe gradient is missing or non-finite." Root cause: the probe hardcoded `torch.float16` regardless of the real training path's `precision="auto"` policy, which prefers bf16 whenever the device supports it (L4 does) because fp16's narrow exponent range can overflow in wide multi-scale modules (PIDNet's SPP) that bf16 would not. Extracted the shared policy into `resolve_auto_precision()` and made the probe use it. Application commit `b22fd123e46478c1d7d368b8fbf50a28dbe28fdd`; Ruff/format/mypy passed; pytest 495 passed/17 skipped without the pinned stack, 510 passed/2 skipped with it; notebook generated twice identically at SHA-256 `1b7349e03a65dab05ee1a9a3ace840e65ac540be0c50457f01f2691ba4975559` | User supplied the real Colab failure log and asked for continued autonomous progress toward a real training run | Local correction and notebook repin complete; **this specific fix has not been verified on real CUDA hardware** (none available in this environment) — the next real L4 run is the actual test | This fix is reasoned from the failure log and the codebase's own stated precision policy, not empirically reproduced locally, unlike the earlier PIDNet-S GenerateEdge fix which was reproduced against the real MMSeg checkout on CPU. No GPU training, scientific metric, accepted release, merge, push, or tag is claimed by this entry. |
| 2026-08-08 | Claude Code | Diagnose and fix a real L4 production-pipeline failure (`Pad` transform has no `seg_pad_val` kwarg) | `stabilize/colab-v2` | `src/edgeguard/rescue/mmseg_runtime.py` (`_evaluation_pipeline`, `_inference_pipeline` `Pad` dict shape), master notebook repin | User ran the notebook again on a real Colab L4 after the AMP fix; `five-model-runtime-canary` passed outright for all five architectures (`fp16_finite_model_count: 5`, GPU `NVIDIA L4`), full Cityscapes+IDD20K data staging completed, then `production-pipeline` failed building `val_dataloader` for the first stage (`smoke`/`segformer_b0`) with `TypeError: Pad.__init__() got an unexpected keyword argument 'seg_pad_val'`. Root cause: `_evaluation_pipeline()` passed `pad_val` and `seg_pad_val` as two separate kwargs to the `Pad` transform, but the pinned mmcv-lite `Pad` (`mmcv/transforms/processing.py`) has no `seg_pad_val` parameter — only a single `pad_val`, either a number or `dict(img=..., seg=...)`. Fixed by passing `pad_val={"img": 0, "seg": config.ignore_index}`; made `_inference_pipeline()`'s bare `pad_val=0` explicit as `{"img": 0}` for consistency (behavior unchanged). Application commit `ff2642265a3cf377988de30a844a13a33ec34238`; Ruff/format/mypy passed; pytest 485 passed/15 skipped without the pinned stack, 500 passed/0 skipped with it; notebook generated twice identically at SHA-256 `d6a640ce064fc5e8fa252069d597f86a68a2059f9a2668bf6ada7a2bc3b2bd16` | User supplied the real Colab failure log and asked for continued autonomous progress toward a real training run | Local correction and notebook repin complete; reproduced and fixed against the real pinned MMSeg checkout (`Compose()` with the mmseg registry scope active, matching how real training builds pipelines), but **not yet verified on real L4 hardware** — the next real L4 run is the actual test | The AMP stack-probe fix from the prior entry is now confirmed on real hardware. This fix is reproduced locally against the exact pinned checkout, unlike the AMP fix which was reasoned-only until this run. No GPU training, scientific metric, accepted release, merge, push, or tag is claimed by this entry. |
| 2026-08-09 | Claude Code | Diagnose and fix a real L4 resume failure (`last_checkpoint` marker used a bare filename) | `stabilize/colab-v2` | `src/edgeguard/rescue/mmseg_components.py` (`EdgeGuardRecoveryHook.after_train_iter` marker write), `src/edgeguard/rescue/mmseg_runtime.py` (Drive cross-session recovery marker write), new `tests/unit/test_mmseg_recovery_checkpoint_marker.py`, master notebook repin | User ran the notebook again after the `Pad`/`seg_pad_val` fix; `five-model-runtime-canary` passed, data staging reused frozen candidates, and `smoke`/`segformer_b0` training actually started and ran to the intentional interruption at optimizer step 25 (by design, to prove interruption/resume). The resume subprocess then failed with `FileNotFoundError: recovery_25.pth can not be found.`. Root cause: `EdgeGuardRecoveryHook.after_train_iter` wrote only the bare checkpoint filename into `<work_dir>/last_checkpoint`; this codebase's own reader (`latest_checkpoint()` in `colab_recovery.py`) resolves a relative marker against `work_dir` defensively, but MMEngine's own built-in auto-resume (`Runner.load_or_resume()` → `find_latest_checkpoint()`) returns the raw marker content verbatim and resolves it relative to the process's cwd, which is the project root for every child process `colab_pipeline.py` spawns, not the run's `work_dir`. The identical bug existed in `train_model`'s Drive cross-session recovery path, the exact mechanism this recovery system exists to survive a dead Colab session. Fixed both write sites to write the absolute path, matching MMEngine's own `CheckpointHook` convention; confirmed pathlib join with an absolute right-hand side leaves every existing bare-filename reader in this codebase unaffected. Application commit `e3f3159a30372997aba00ad5543cdb0cd27a45ab`; Ruff/format/mypy passed; pytest 485 passed/16 skipped without the pinned stack, 501 passed/0 skipped with it, including a new regression test that reproduces the exact failure via MMEngine's real `find_latest_checkpoint()` and is confirmed to fail on the pre-fix code and pass on the fix; notebook generated twice identically at SHA-256 `2632be291b08cda6a67a1497e4d2b2c3bbac57c08047088b6c6867754ae1acbc` | User supplied the real Colab failure log and asked for continued autonomous progress toward a real training run | Local correction and notebook repin complete; reproduced and fixed against the real pinned MMSeg/MMEngine stack, but **not yet verified on real L4 hardware** — the next real L4 run is the actual test | Both the AMP stack-probe fix and the `Pad`/`seg_pad_val` fix from the two prior entries are now confirmed on real hardware (training actually started and reached the interruption point). This fix is reproduced locally against the exact real MMEngine auto-resume function involved in the crash. No GPU training, scientific metric, accepted release, merge, push, or tag is claimed by this entry. |
| 2026-08-09 | Claude Code | Diagnose a fourth real Colab-path bug (`Pad` size dimension order) by building a real local CPU orchestrator rehearsal instead of continuing to push-and-wait | `stabilize/colab-v2` | `src/edgeguard/rescue/mmseg_runtime.py` (`_evaluation_pipeline`/`_inference_pipeline` `Pad` size order), `tests/unit/test_mmseg_real_training_step.py` (new orientation regression test), `tests/support/tiny_pipeline_fixture.py` (new), `tests/integration/test_colab_pipeline_cpu_rehearsal.py` (new), `.github/workflows/semantic-framework-cpu-probe.yml`, master notebook repin, `docs/canonical-colab-runbook.md` pre-push checklist | User asked, after three straight real-Colab-only bug discoveries, whether the answer was more local testing, deeper Drive/notebook knowledge, downloading data locally, or a notebook/file-architecture rewrite. Two Explore agents + one Plan agent (in plan mode) established with file:line evidence that all three prior bugs were training-pipeline bugs, none were Drive/staging bugs, and that the project's own "claim-safe local cell execution" check is a stub that hardcodes `{"scientific_status": "not_run"}` and never runs `Runner.train()`, `EdgeGuardRecoveryHook`, or `val_dataloader` — nothing in the repo had ever driven the real orchestrator end to end. Built `tests/support/tiny_pipeline_fixture.py` + `tests/integration/test_colab_pipeline_cpu_rehearsal.py`, which drives the real subprocess-spawning `ColabPipeline` through a real smoke-stage interrupt+resume cycle on CPU with tiny fixture data. Building it immediately surfaced a fourth real bug (confirmed via isolated `mmcv.transforms.Pad` reproduction and full end-to-end runs, both failing pre-fix and passing post-fix): `Pad`'s `size` constructor argument is documented `(w, h)` and internally reversed before calling `mmcv.impad(shape=...)`, which expects `(h, w)`; `_evaluation_pipeline`/`_inference_pipeline` passed `config.crop_size` (this codebase's `(h, w)` convention) directly, silently transposing the pad target — invisible for the square-crop cases tested so far, but not for real Cityscapes' non-square 512x1024 crop. No prior real Colab run had ever reached validation to expose it. Application commits `d7a4430` (Pad fix + regression test) and `c4008d9` (rehearsal harness + CI wiring); Ruff/format/mypy passed; pytest 485 passed/17 skipped without the pinned stack, 502 passed/0 skipped with it; the new rehearsal test's fast tier (3 core models: segformer_b0, fast_scnn, pidnet_s) confirmed passing end to end against the real pinned stack locally (~8 minutes), including a real computed mIoU at the final validation step for every model; notebook generated twice identically at SHA-256 `20aca870baa26c0991cb5c547827f084984e9250c50110f3222cfef4fd26ff94` | User explicitly asked for a diagnosis-and-strategy session (not just another Colab retry) before the next push, entered plan mode, and approved the resulting plan, including pushing the already-queued `last_checkpoint` fix immediately while the rehearsal harness was built in parallel | Local correction, regression tests, and rehearsal harness complete; CI wiring complete but not yet exercised by a real CI run at this commit; not yet confirmed on real L4 hardware | This is the first bug in the session discovered without spending any real Colab GPU time — found and fixed entirely from a local CPU rehearsal before the corresponding commit was ever pushed. The `AMP`, `Pad`/`seg_pad_val`, and `last_checkpoint` fixes from the three prior entries are now all confirmed on real L4 hardware. No GPU training, scientific metric, accepted release, merge, push, or tag is claimed by this entry. |
| 2026-08-09 | Claude Code | Diagnose and fix a fifth real Colab-path bug (stale cross-commit Drive recovery checkpoint crashing the campaign before any training step ran), and answer the user's request for a general strategy on this class of error | `stabilize/colab-v2` | `src/edgeguard/rescue/mmseg_runtime.py` (`train_model` speculative-vs-explicit resume handling), `src/edgeguard/rescue/colab_recovery.py` (new `peek_recovery_metadata`), `tests/integration/test_colab_pipeline_cpu_rehearsal.py` (new stale-recovery regression test), `docs/canonical-colab-runbook.md` pre-push checklist, master notebook repin | User ran the newly-pushed `c4008d9` on real L4: the five-model canary and data staging both passed cleanly, but `production-pipeline` crashed on the very first `smoke`/`segformer_b0` attempt — no training step ever ran — with `ValueError("Drive recovery checkpoint belongs to a different immutable run")` at `mmseg_runtime.py:890`. User asked both to fix this specific error and to develop a general strategy for anticipated/unanticipated Colab errors, noting frustration that failures kept recurring despite substantial prior validation work. Entered plan mode; one Explore agent confirmed the mechanics with file:line evidence, one Plan agent designed the fix. Root cause: on a fresh Colab session (no local `run_identity.json`), `ColabPipeline._run_training_phase` decides to append `--resume` purely because a Drive recovery pointer *file* exists (`_recovery_pointer_exists` has zero identity awareness); `train_model()`'s `identity` dict includes `project_commit` among ~20 fields, so every commit invalidates every previously-published Drive checkpoint's `identity_sha256` campaign-wide, even for unrelated stages/models; `train_model()` then correctly detected the mismatch but incorrectly treated it as fatal, crashing the whole 5-model campaign instead of abandoning a speculative resume that turned out stale. Fixed by distinguishing this speculative auto-resume (no local run ever existed) from an explicit resume of a known local run: the speculative path now degrades gracefully (writes `stale_recovery_skipped.json`, proceeds fresh) instead of raising; the explicit local-resume identity check is untouched, since that one is a real safety property. Added `peek_recovery_metadata()` to read pointer receipt metadata cheaply. Added a regression test that publishes a Drive pointer with a fabricated mismatched identity before running the pipeline; confirmed via a temporary `git stash` of only the two fix files that it fails pre-fix with the identical real-Colab error message, and passes with the fix restored. Strategy delivered to the user: all five bugs found this session are in this project's own bespoke orchestration/recovery layer, none in MMSeg/PyTorch/Cityscapes itself; the CPU rehearsal harness (built earlier this session) already closes most of that gap, but this bug specifically evaded it because every existing rehearsal test starts from a clean, empty recovery store — the new test closes that specific coverage gap. Flagged, but explicitly left unimplemented pending human-owner sign-off per project governance: whether `project_commit` should remain part of the strict identity-compare value at all, versus becoming provenance-only metadata. Application commit `3262af8`; Ruff/format/mypy passed; pytest 485 passed/17 skipped without the pinned stack, 19/19 passed in the mmseg-gated suite with it (including the new stale-recovery test); all three CPU rehearsal tests confirmed passing end to end against the real pinned stack locally (~24 minutes combined); notebook generated twice identically at SHA-256 `ad026d4ec1209b40c2b8a8a5499cb19111403f26adc272792cce17dd1ffa4944` | User asked for both a point fix and a general strategy; entered plan mode, reviewed the plan (including the explicit "not implemented, needs your decision" carve-out for the `project_commit` identity-scoping question), and approved via ExitPlanMode | Local correction, regression test, and doc updates complete; not yet confirmed on real L4 hardware — the next real Colab attempt is the actual test | All four prior bugs (AMP, `Pad`/`seg_pad_val`, `last_checkpoint`, `Pad` orientation) remain confirmed on real L4 hardware. This fix has not yet had a real-hardware confirmation. No GPU training, scientific metric, accepted release, merge, push, or tag is claimed by this entry. |
| 2026-08-09 | Claude Code | Diagnose and fix a sixth real Colab-path bug (checkpoint RNG-state device mismatch), contain the intentional-interrupt self-test so it can no longer kill the whole campaign, and update project governance to reflect standing engineering/push authority on this branch | `stabilize/colab-v2` | `src/edgeguard/rescue/mmseg_components.py` (`_cpu_rng_state` helper, device-agnostic RNG save/load), `src/edgeguard/rescue/colab_pipeline.py` (`PipelineInputs.recovery_self_test_model`, once-per-campaign self-test gating, `_restart_after_failed_self_test`, `_verify_recovery_self_test_not_failed` pre-pilot gate), `tests/unit/test_mmseg_recovery_rng_state.py` (new), `tests/unit/test_colab_pipeline.py` (new self-test-containment tests), `tests/unit/test_semantic_first_rescue.py` (new AMP config-shape test), `tests/integration/test_colab_pipeline_cpu_rehearsal.py` (updated resume-count assertions), `CLAUDE.md`, master notebook repin, all four doc files | User ran the newly-pushed `3262af8` on real L4: canary, data staging, and a fresh `smoke`/`segformer_b0` start plus the intentional interruption at optimizer step 25 all succeeded (three prior fixes confirmed), then the resume subprocess crashed with `TypeError: RNG state must be a torch.ByteTensor in EdgeGuardRecoveryHook`. User granted standing engineering/push authority on this branch ("godmode") and asked for a from-scratch review of Drive/Colab/model handling, explicitly authorizing a rewrite if the design was found illogical. Ran two Explore agents in parallel (one on the RNG hook mechanics, one on the broader bespoke-orchestration-layer architecture) plus one Plan agent. Root cause: `Runner.resume()` loads checkpoints with `map_location=get_device()` (CUDA on Colab), moving every tensor in the pickle — including the RNG state `EdgeGuardRecoveryHook.before_save_checkpoint` stores — onto that device; `torch.set_rng_state`/`torch.cuda.set_rng_state` reject anything but a CPU uint8 tensor. Confirmed mmengine 0.10.7 has no RNG save/restore of its own, so the hook is genuine capability and was not deleted; both save and load paths now coerce every RNG tensor to CPU/uint8. Reproduced and regression-tested with no GPU: `mmengine.device.get_device()` returns `"mps"` on this dev machine, a real foreign device relative to a CPU tensor, so `.to("mps")` reproduces the identical error; confirmed via `git stash` to fail pre-fix and pass post-fix. This also surfaced why the CPU rehearsal harness missed it: a local, never-committed `sitecustomize.py` had been forcing `mmengine.device.utils.DEVICE = "cpu"` to route around unrelated MPS operator gaps, which incidentally hid every device-class bug too — removed that forcing (kept only the unrelated `torch.load(weights_only=...)` patch) and documented the distinction in `canonical-colab-runbook.md`. Architecture audit found all six of this session's bugs live in the project's own bespoke orchestration/recovery layer (none in MMSeg/PyTorch/Cityscapes), that the Drive content-addressed store produced zero of them and was left untouched, and that the one genuinely illogical piece was the intentional-interrupt self-test running once per model (5 deliberate crash+resume cycles per campaign, no disable switch) — three of six bugs surfaced through it. Made it run once per campaign (`PipelineInputs.recovery_self_test_model`, default the first core model); a failed resume leg now writes hash-sealed evidence and restarts the model instead of killing the campaign, while `pilot`/`screening`/`hpo`/`final` refuse to start with any unresolved failure record. Also added a CPU-visible config-shape test for the `AmpOptimWrapper` branch, dead on every CPU run since `resolve_auto_precision` always returns `fp32` without CUDA — exactly how this session's first bug escaped local testing. Updated `CLAUDE.md` to reflect the granted authority while explicitly keeping the non-fabrication contract, ADR-0005 sealed-test gate, and `AI_USAGE_LOG.md` append-only discipline intact. Application commit `c88ac8f`; Ruff/format/mypy passed; pytest 489 passed/19 skipped without the pinned stack, 22/22 in the mmseg-gated suite with it; all three CPU rehearsal tests confirmed passing end to end against the real pinned stack on the real MPS device (~35 minutes combined, not artificially forced to CPU); notebook generated twice identically at SHA-256 `4b853a8d6acf70e4b94d828cbb77672b92b1d017317425f755bb80b6caeb9d17` | User granted standing engineering/push authority for this branch ("sana bırakıyorum tüm kararları... godmode") but the CLAUDE.md update explicitly preserves the boundaries the human owner's own governance file already drew (non-fabrication, sealed test, dataset roles, scientific conclusions) rather than removing them, on the grounds that removing them would work against the user's actual goal of a defensible thesis | Local correction, regression tests, self-test containment, and doc updates complete; not yet confirmed on real L4 hardware — the next real Colab attempt is the actual test | All five prior bugs (AMP, `Pad`/`seg_pad_val`, `last_checkpoint`, `Pad` orientation, stale Drive recovery) remain confirmed on real L4 hardware; the stale-Drive-recovery fix specifically got a fresh smoke start and a step-25 interruption to succeed before this newest bug was reached. This fix and the self-test containment change have not yet had real-hardware confirmation. No GPU training, scientific metric, accepted release, merge, push, or tag is claimed by this entry. |
| 2026-08-09 | Claude Code | Diagnose and fix a seventh real Colab-path bug (PIDNet `BoundaryLoss` bf16 dtype mismatch), and answer the user's question about whether low smoke-stage mIoU indicates a data/model problem | `stabilize/colab-v2` | `src/edgeguard/rescue/mmseg_components.py` (new `EdgeGuardBoundaryLoss` registration), `tests/unit/test_pidnet_boundary_loss_dtype.py` (new), master notebook repin, all four doc files | User ran the newly-pushed `2b078d3` on real L4 and reached the furthest point yet: `segformer_b0` completed its full smoke cycle end to end (fresh start, interruption at step 25, real resume via the sixth fix, training to step 50, validation, mIoU 6.53%), and `fast_scnn` trained straight through (mIoU 0.07%) under the now-once-per-campaign self-test — confirming bugs 1-6 all hold together on real hardware. Then `pidnet_s` crashed on its first training step with `RuntimeError: Index put requires the source and destination dtypes match, got BFloat16 for the destination and Float for the source` inside `mmseg/models/losses/boundary_loss.py:52`. User also asked, given the low mIoU numbers and repeated Colab errors, whether Cityscapes/IDD20K/BDD were sufficient, whether the data was processed wrong, or the models chosen wrong. Root cause of bug 7 (one Explore agent, reading the pinned upstream source): `BoundaryLoss.forward` builds `weight = torch.zeros_like(log_p)`, and under real `AmpOptimWrapper(dtype='bfloat16')` autocast on real CUDA, `log_p` (boundary-head raw logits) is already bfloat16; the ratio assigned into `weight` comes from summing a float32 label mask autocast never touches, so it stays float32 — `index_put_` rejects the mismatch on the pinned Colab torch (2.1.1+cu121). A grep of the pinned checkout confirmed no other loss used by any of the five models shares this `zeros_like`-then-index-assign pattern. Fixed by registering `EdgeGuardBoundaryLoss` via this repo's existing `force=True` override idiom (already used four times in the same file) — identical to upstream except two `.to(weight.dtype)` casts before the indexed assignment; only `pidnet_s` uses `BoundaryLoss`. Confirmed this dev machine's torch (2.13.0) silently permits the exact implicit downcast that torch 2.1.1 rejects (checked directly against both `Tensor.__setitem__` and `Tensor.index_put_`) — a torch-version difference, not a device one — so unlike bugs 3/5/6, this one cannot be reproduced as a "raises pre-fix" test locally even in principle; the new test instead asserts the fix's actual guarantee (dtype-aligned output, finite loss, and bit-identical numerics to upstream when dtypes already match), and `git stash` confirmed the override registration itself is present only post-fix. Re-ran the fast-tier CPU rehearsal (a real `pidnet_s` smoke run, fp32, unaffected by this bf16-only bug) end to end to confirm no regression. Answered the data/model question directly: the low mIoU figures are `smoke`-phase results — a deliberately meaningless 50-step run whose only purpose is proving the orchestration mechanics work (`run_manifest.json` records `scientific_status: "not_run"`); real training (`pilot` 2000 steps through `final` 40000 steps) has not started because every campaign attempt has died in the orchestration/precision layer, not in training science, and all seven bugs found this session are in that layer or in an untested corner of pinned MMSeg (bf16 + BoundaryLoss), none in Cityscapes/IDD20K or the five-model architecture choice, which remain ADR-0009-frozen scientific decisions outside engineering scope. Application commit `4917c49`; Ruff/format/mypy passed; pytest 489 passed/21 skipped without the pinned stack, 24/24 in the mmseg-gated suite with it; notebook generated twice identically at SHA-256 `6b1476c4c1eec40370673ae054b54952086a982031db6b0cff6e07734c5a688c` | Continuing standing engineering/push authority granted for this branch; entered plan mode for the diagnosis, presented the fix and the direct answer to the data/model question, user approved via ExitPlanMode | Local correction, regression test, and doc updates complete; not yet confirmed on real L4 hardware — this fix depends on real bf16 autocast on real CUDA, which cannot be verified any other way | All six prior bugs (AMP, `Pad`/`seg_pad_val`, `last_checkpoint`, `Pad` orientation, stale Drive recovery, RNG checkpoint device) remain confirmed on real L4 hardware; this fix is the first this session that is structurally unconfirmable outside real Colab. No GPU training, scientific metric, accepted release, merge, push, or tag is claimed by this entry. |
| 2026-08-09 | Claude Code | Full technical-takeover audit at the user's explicit request (act as Principal CV/DL/MLOps/Jetson engineer plus hostile reviewer, audit with no loyalty to prior decisions, issue an architecture verdict, repair/replace/rebuild as evidence demands); fix the one real methodology defect found (uniform AdamW optimizer across 5 architecturally different models); retarget CI to the real working branch; document data-inventory tooling usage | `stabilize/colab-v2` | `src/edgeguard/rescue/mmseg_runtime.py` (new `resolve_model_optimizer_defaults`, per-model optimizer wiring in `build_training_config`/`train_model`), `.github/workflows/semantic-framework-cpu-probe.yml` (push-trigger branches), `docs/canonical-colab-runbook.md` (data-inventory section), `tests/unit/test_mmseg_real_training_step.py` (two new tests), `tests/unit/test_semantic_first_rescue.py`, `tests/unit/test_cpu_probe_workflow.py`, master notebook repin, all four doc files | User pasted an extensive "Full Technical Takeover" prompt granting simultaneous ownership across CV/segmentation research, data-centric AI, MLOps/reproducibility, and Jetson/TensorRT engineering, explicitly demanding a forensic audit (git state, entrypoints, dependencies, model registry, losses, HPO, ONNX, TensorRT, notebooks, tests, Drive integration), a KEEP/REPAIR/REPLACE/DELETE classification, an architecture verdict A/B/C/D, and direct implementation of the resulting P0/P1 recovery items rather than only recommendations. Plan mode was system-enforced, so the audit and recovery plan were produced and approved via `ExitPlanMode` before any code changed. Three parallel read-only Explore agents covered repo hygiene/CI/ADRs, model/reliability/HPO/ONNX/TensorRT/Jetson, and data ontology/notebook/Drive architecture, respectively — verdict: **B, sound concept with bounded defects**, not the "repeatedly failed" prior the prompt's own framing assumed, since ADR-0008/0009 (2026-07-28, amended 2026-07-30) had already closed the Codex-era instability the prompt described. Reading all 5 pinned upstream MMSeg configs directly (not trusting a sub-agent summary) found the one real defect: `build_training_config` blanket-overwrote every model's `optim_wrapper` with a shared `AdamW(lr=6e-5, wd=0.01)`; only `segformer_b0` actually matches this recipe, while `fast_scnn`/`pidnet_s`/`ddrnet_23_slim`/`bisenetv2` natively train with SGD+momentum at learning rates 150-2000x higher, in the wrong optimizer family entirely — Optuna's HPO space only ever searched within a fixed AdamW assumption and could never have self-corrected this. Fixed with a new `resolve_model_optimizer_defaults()` that reads each model's real upstream `optim_wrapper.optimizer` as the training baseline; explicit overrides (what every HPO trial already supplies) still apply on top, now preserving the model's native optimizer type/momentum. Deliberately left untouched: the frozen HPO learning-rate/weight-decay search range (tuned for AdamW-scale), flagged as an open question for the SGD-native models once real HPO execution begins rather than silently resolved, since HPO scope is reserved for the human owner per this project's own `CLAUDE.md`. Also retargeted `semantic-framework-cpu-probe.yml`'s push trigger from the stale `feat/first-vertical-slice` to `stabilize/colab-v2`/`main` (this CI job runs the exact CPU rehearsal that caught most of this session's real bugs, and had never run automatically on the branch all current work happens on), and documented the existing `scripts/audit_dataset.py` data-inventory tool's invocation in the runbook (no artifacts claimed locally — no real dataset on this dev machine by design). The audit confirmed the dataset ontology, reliability/OOD stack, dependency tri-tier separation, single generated notebook, and ONNX/TensorRT/Jetson code were all real, tested, and already matching the prompt's own asks — left untouched, with the plan's KEEP/REPAIR/REBUILD table and full evidence recorded for the record. Application commit `a50b635`; Ruff/format/mypy (`mypy src/edgeguard`, matching `ci.yml`) passed; pytest 499 passed/32 skipped without the pinned stack, 27/27 in the mmseg-gated test files with it (up from 24, adding a per-model native-optimizer parametrized test and an end-to-end config-wiring test); noted but not fixed as out of scope — running the *entire* suite with the checkout set surfaces 12 pre-existing, order-dependent failures in `tests/unit/test_dataset_preparation.py` that don't reproduce in isolation and have no connection to this session's changes; the full 3-test CPU rehearsal suite was re-run end to end and passed (18m40s); notebook generated twice identically at SHA-256 `263e9c1efec73ee18778ac460133c9fabc71e248d475250c3eccc153a72bc43b` | User's takeover prompt explicitly demanded direct implementation of P0/P1 items rather than only a report; plan mode intervened regardless (system-enforced), so the full audit and a scoped "IMMEDIATE ACTION" implementation plan were presented and approved via `ExitPlanMode` with no changes requested, then implemented as approved | Local correction, regression tests, CI retarget, data-inventory documentation, and doc updates complete; not yet confirmed on real L4 hardware — the optimizer-family change affects 4 of 5 models' actual training dynamics, so the next real Colab `smoke`/`pilot` run is the load-bearing test | All seven prior Colab-path bugs remain at their previously-recorded confirmation status (six confirmed on real L4, the `BoundaryLoss` bf16 fix still unconfirmable outside real CUDA). This audit found no component meeting the bar for REBUILD/REPLACE and made no scientific-scope decision (5-model comparison, dataset roles) — both are explicitly left to the human owner. No GPU training, scientific metric, accepted release, merge, push, or tag is claimed by this entry. |
| 2026-08-09 | Claude Code | Assign roles to four newly-uploaded auxiliary dataset sources (WildDash 2's two packages, WildDash's pickup/van ontology-extension script, RailSem19) under an explicit narrow delegation from the user, and confirm Colab readiness | `stabilize/colab-v2` | `configs/dataset/colab_data_access_v1.yaml` (`wilddash2.packages`, new `wilddash2.engineering_packages`, two new `excluded_sources` entries), `docs/adr/ADR-0009-multidomain-generalization.md` (2026-08-09 role amendment), `CLAUDE.md` (narrow delegation note), `docs/AI_USAGE_LOG.md` | User uploaded 11 new archives to Drive (ACDC's two required packages plus 4 unused extras, already covered by the existing frozen config; WildDash 2's `wd_both_02.zip`/`wd_public_v2p0.zip`; WildDash's `wd_add_pickupvan.zip`; RailSem19's `rs19_val.zip`), verified upload integrity by size against local originals (all 11 matched exactly, including the two largest at 15.59 GB and 5.02 GB), then explicitly asked Claude Code to decide how all of them should be used and whether the project is ready for a real Colab run ("nasıl kullanılacağına sen karar ver, uzman ve yönetici sensin"). Entered plan mode (system-enforced); grounded the decision in direct reads of `colab_data_access_v1.yaml`, `semantic_ontology_v2.yaml`, and `src/edgeguard/rescue/colab_data.py`'s validation logic plus its existing test coverage. Decisions made: `wd_both_02.zip` (812 images, no ground truth) activates the already-frozen `primary_sealed_external` WildDash 2 role verbatim — just fills in the real filename. `wd_public_v2p0.zip` (4256 images, WITH ground truth) is added as `engineering_packages`/`scientific_eligible: false`, mirroring the existing `bdd100k` Kaggle-mirror pattern exactly, because the ontology already declares `wilddash2.mask_encoding: server_only` — decoding this GT for any metric would silently break the sealed claim on the real submission. `wd_add_pickupvan.zip` (an ontology-extension script adding pickup/van/autorickshaw classes) is excluded: the ontology already deliberately ignores `autorickshaw` for IDD20K under ADR-0009, and applying this mid-campaign would invalidate every already-computed manifest/split hash; retained only as a future v2-ontology candidate. RailSem19 is excluded from training/eval scope as off-domain rail imagery with weakly-supervised road labels; noted only as a possible future zero-cost OOD-diagnostic stress input to the existing `evaluation/ood.py` stack, not scheduled. Also answered the Colab-readiness question directly: none of this dataset-triage work blocks a real run — ACDC only activates `after_model_freeze`, WildDash2/RailSem19 were never wired into the frozen `configs/rescue/semantic_first.yaml` training scope, and all seven real bugs plus the optimizer-family fix from the prior two rounds are already pushed at `f9a0902`; the next action is a human-executed Colab L4 Run all, watching specifically whether all five models' `smoke` stage now completes and whether the four newly-SGD models' loss curves look stable. No change to `configs/rescue/semantic_first.yaml` or its frozen assertions; no change to `src/edgeguard/rescue/colab_data.py` itself, only to data already consumed by its existing, tested `engineering_packages`/`excluded_sources` schema. Ruff/format/mypy passed; `pytest tests/unit/test_colab_data.py` passed, confirming the new YAML entries validate against the schema `colab_data.py` enforces | User explicitly delegated this specific role-assignment decision in chat ("sen karar ver, uzman ve yönetici sensin"), scoped narrowly to these four auxiliary sources; recorded as an explicit, narrow lift in both `CLAUDE.md` and an ADR-0009 amendment rather than acted on silently — the core `train_fit`/`train_select`/official-validation roles for Cityscapes/IDD20K remain reserved and untouched. Entered plan mode for the decision + implementation, user approved via `ExitPlanMode` | Config/ADR/CLAUDE.md/log updates complete and locally verified; no training/scientific artifact created or claimed | This is an acquisition-config and documentation change only — no dataset was downloaded, no model trained, no manifest or ontology hash changed, no scientific-scope decision made (the 5-model comparison and core dataset-role boundaries are untouched). No merge, tag, or GPU run is claimed by this entry. |
| 2026-08-12 | Claude Code | Diagnose and fix an eighth real Colab-path bug (`clip_grad.max_norm=float("inf")` doesn't survive `mmengine.Config` dump/reload round-trip, crashing the screening-evidence evaluation step) | `stabilize/colab-v2` | `src/edgeguard/rescue/mmseg_runtime.py` (`clip_grad.max_norm` sentinel), `tests/unit/test_semantic_first_rescue.py` (new dump/reload round-trip regression test), master notebook repin, all four doc files | User ran the newly-pushed `a50b635` on real L4 and, for the first time in this project's history, got all five models through a full real 6000-step `screening` run (real measured mIoU: segformer_b0 16.48%, fast_scnn 20.53%, pidnet_s 26.54%, ddrnet_23_slim 25.21%, bisenetv2 17.41%, ~20 hours combined compute) before the pipeline crashed with `NameError: name 'inf' is not defined` in the screening-evidence evaluation step, and pasted the full failure log unprompted while the campaign was live. Root cause, found by reading the traceback directly: `build_training_config()`'s `clip_grad.max_norm` was `float("inf")` (`error_if_nonfinite=True` is the real safety net; `max_norm` was never meant to actually bind). `train_model()` dumps the resolved config to `resolved.py` via `mmengine.Config.dump()` before every training call, including no-op resume-and-skip re-entries; `scripts/evaluate.py` (used by the screening/final evidence phases — the first phase in this project to ever reload a dumped config from disk rather than use the in-memory `cfg`) reloads it via `mmengine.Config.fromfile()`, which `eval()`s the dumped Python source; mmengine's dumper serializes `float("inf")` as the bare token `inf`, not a valid Python literal without `float(...)`/`math.inf` in scope. This bug was structurally unreachable by any prior test or training stage — none of them ever re-parsed a dumped config from disk — so it survived undetected through all seven prior bug fixes and roughly 20 hours of real L4 compute until the pipeline finally reached this one, previously-unexercised code path. Fixed by replacing `float("inf")` with a large finite sentinel (`1e9`), which round-trips cleanly and stays effectively unbounded for any real gradient norm observed this session (even a visibly diverging `bisenetv2` screening run topped out around `grad_norm ~560`). Reproduced with zero GPU dependency (confirmed directly: `mmengine.Config.dump()` + `.fromfile()` on a config containing `float("inf")` raises the identical `NameError` with no CUDA involved) and regression-tested; confirmed via `git stash` to fail pre-fix and pass post-fix. Also separately noted, not fixed, confirmed pre-existing and unrelated via the same `git stash` check: the full `tests/integration/test_colab_pipeline_cpu_rehearsal.py` suite currently fails on this dev machine with `RuntimeError: view size is not compatible with input tensor's size and stride` during `backward()` — the known MPS operator-gap class of issue already documented in `canonical-colab-runbook.md`, identical on both pre-fix and post-fix code. Confirmed `train_model()` dumps `resolved.py` fresh on every entry (including resume-and-skip), so no manual Drive cleanup is needed for the fix to take effect on the next Colab run. Application commit `2b1ebff`; Ruff/format/mypy passed; full local suite 500 passed/32 skipped without the pinned stack, mmseg-gated test files 27/27 with it (up from 24, adding the dump/reload round-trip test); notebook generated twice identically at SHA-256 `25c7393e4ac216700bba35a9b846ba89bc97d5b32700cca09a0dfad6003334e1` | Continuing standing engineering/push authority for this branch; user shared the live failure log directly, consistent with this session's established diagnose-fix-push loop for real Colab failures — no separate approval requested or required per `CLAUDE.md` | Local correction, regression test, and doc updates complete; the `clip_grad` fix itself is proven independently of the unrelated CPU-rehearsal-suite regression (both directions confirmed via `git stash`); not yet re-confirmed on real L4 hardware — the next real Colab attempt is the load-bearing test, expected to let the screening-evidence step (and all downstream phases: HPO, `final`) complete for the first time | All seven prior Colab-path bugs remain at their previously-recorded confirmation status. This is the first bug this session found *after* real scientific-quality screening-stage evidence was already produced for all five models — that evidence (mIoU figures above) is real, `scientific_status: "measured"`, not fabricated, and is unaffected by this fix (the crash happened in a downstream evaluation step, not during training). No accepted release, merge, or tag is claimed by this entry. |
| 2026-08-12 | Claude Code | Build an automatic, exhaustive, name-agnostic inventory of every raw archive in Drive's `EdgeGuard/private_inputs/` at the user's explicit request | `stabilize/colab-v2` | `src/edgeguard/rescue/archive_inventory.py` (new), `src/edgeguard/rescue/stall_guard.py` (new, extracted from `colab_data.py`), `src/edgeguard/serialization.py` (new `write_verified_zip`), `src/edgeguard/deployment/colab_release.py` (reuses `write_verified_zip`), `scripts/inventory_private_inputs.py` (new CLI), `scripts/dev/build_colab_notebooks.py` (new early notebook cell), `tests/unit/test_archive_inventory.py` (new, 20 cases), `docs/canonical-colab-runbook.md`, master notebook repin, all four doc files | User asked for a full inventory of every zip/archive in `private_inputs/` (byte sizes, image counts, resolutions, class counts, quality) runnable in the master notebook and auto-downloadable at session start, both for the thesis report and to spot future optimization targets, and so Claude Code itself would have full visibility into `private_inputs/`. A plan was proposed and approved, then the user explicitly amended it before implementation began: "basite kaçma... isim verme klasördeki her zipe bakılsın... bil adın gibi... belki ileride dengesiz veri seti mevzusundan kaynaklı class sayımızı düşürebiliriz" — rejecting a bounded-sample design and requiring every file to get identical inspection depth regardless of name, with real *measured* per-class histograms (not declared ontology counts), since the data may inform an actual future class-reduction decision. Implemented `archive_inventory.py`: dispatch is purely on file format (zip/tar.gz/standalone) and, within an archive, purely on entry extension/decoded PIL mode — never on dataset name. Every image entry is fully decoded (`PIL.Image.open(...).load()`, not header-sampled) for real resolution/format/mode counts and corruption detection; every entry whose mode is label-like (`L`/`P`/`I`/`1`) gets a real per-class pixel- and image-count histogram via `numpy.unique(..., return_counts=True)`. Declared metadata from `colab_data_access_v1.yaml` is attached afterward as annotation only, matched by exact filename, never used to gate or shorten the scan; an undeclared file is reported as having no known role rather than guessed — verified by a dedicated test that a plausibly-related but undeclared filename is not matched. `record_type: "raw_archive_inventory"` carries no `scientific_status` field (engineering/audit artifact). Deliberately not a new `ColabPipeline` phase and not gated behind the accepted-release-gated `package` phase (`build_colab_release_packages` requires a fully accepted 5-model release) — that gate would make the report unavailable for weeks given how often real sessions die mid-campaign; instead it is a new, independent, early notebook cell wrapped in try/except-and-continue, run right after checkout and before the main campaign subprocess, so it can never block or fail the real campaign. The report directory is content-addressed from `private_inputs/`'s file listing (names+sizes+mtimes) so an unchanged folder reuses the prior session's report instead of re-scanning every file again — verified end to end via a CLI subprocess test (scan, cache-hit reuse, cache-miss on folder change). Extracted `stall_guard.py` (Drive/FUSE read-stall guard, previously inlined in `colab_data.py`) and `write_verified_zip()` (previously `colab_release.py`'s private `_zip_members()`) as shared infrastructure, refactoring both existing call sites to use them with no behavior change. Application commit `7b604c9`; Ruff/format/mypy passed (118 modules, up from 116); full local suite 520 passed/32 skipped (up from 499/32 — 20 new cases); this commit touches no training/mmseg code path so the mmseg-gated suite and CPU rehearsal suite were not re-run; notebook generated twice identically at SHA-256 `b06b373a2c5c3ab9e1a02f891abcc9d1973655cb69fb1d04281f3f24ddcd6e8d` (5 code cells, up from 4); `tests/integration/test_notebook.py` and the local claim-safe execution harness both pass, confirming the new cell correctly no-ops under `LOCAL_TEST_MODE` | User proposed the feature, approved the initial plan, then explicitly amended it (quoted above) before any scanning code was written; the amendment was incorporated into the plan before implementation began, so no work was discarded. Continuing standing engineering/push authority for this branch per `CLAUDE.md` | Local correction, new module, CLI, notebook cell, tests, and doc updates complete; not yet exercised against a real Drive-mounted `private_inputs/` folder or real archive files — only unit-tested against small synthetic zip/tar fixtures and the CLI subprocess path; the next real Colab session is the first real-data test, and is expected to run automatically and non-fatally regardless of the main campaign's outcome | Purely additive: does not touch `ColabPipeline`/`PHASES`, the `package` phase, or any dataset role/scope decision; does not affect the in-progress real-L4 campaign state. No scientific metric, accepted release, merge, or tag is claimed by this entry. |
| 2026-08-12 | Claude Code | Diagnose and fix a real-Colab bootstrap bug in the newly-shipped private_inputs inventory cell (`ModuleNotFoundError: No module named 'edgeguard'`), reported live by the user | `stabilize/colab-v2` | `scripts/dev/build_colab_notebooks.py` (scoped `inventory_environment` with `PYTHONPATH`, dependency `pip install` before invoking the CLI), `tests/integration/test_notebook.py` (word-boundary PYTHONPATH guard, new assertions), `tests/unit/test_delivery_notebooks.py` (stale `code_cell_count == 4` corrected to 5), master notebook repin, all four doc files | User ran the newly-pushed `7b604c9` on real L4 and pasted the traceback: the new private_inputs inventory cell crashed immediately with `ModuleNotFoundError: No module named 'edgeguard'`, caught by the cell's own try/except (campaign unaffected, exactly as designed, but no report was produced). Root cause: the cell invoked `scripts/inventory_private_inputs.py` via bare `/usr/bin/python3`, mirroring how `run_colab_master.py` is invoked — but `run_colab_master.py` deliberately never imports `edgeguard` at its own top level (it only sets `PYTHONPATH` for the children it spawns later, inside its own `_runtime_environment()`), while the new CLI imports `edgeguard.rescue.archive_inventory` immediately, and nothing had ever put `src/` on that bare interpreter's `sys.path`. Fixed by giving the inventory subprocess its own scoped environment dict (`inventory_environment`, deliberately not the shared `environment` dict used for the main `run_colab_master.py` call) with `PYTHONPATH` set to `src/`, and by `pip install`-ing the small set of pure-Python packages (`numpy`/`Pillow`/`pydantic`/`PyYAML`, at `pyproject.toml`'s pinned ranges) the import chain needs, since this step deliberately runs before the locked training runtime is provisioned and cannot assume Colab's bare system Python already has them — a materially lower-risk category of host-touching than the CUDA/torch locked-runtime isolation this project hardened earlier (small pure-Python packages into the notebook's own already-mutable per-session system Python, not the version-pinned ML stack). Verified directly, not just by assertion: built a throwaway venv with only the four light packages installed and no `edgeguard-road` install at all, ran the CLI against it with only `PYTHONPATH` set, and confirmed a correct report — the same shape as what the notebook cell now does. `tests/integration/test_notebook.py`'s existing `'environment["PYTHONPATH"]' not in source` guard (added 2026-08-06 to keep the *shared* `environment` dict feeding `run_colab_master.py` PYTHONPATH-free) used a bare substring check that also matched `inventory_environment["PYTHONPATH"]`; tightened to a word-boundary regex so it still guards the thing it always meant to guard without banning a correctly-scoped, unrelated dict. A first attempt at the fix's explanatory code comment accidentally contained the literal banned substring `"pip install -e"` in prose (negating it), tripping the same guard plus an equivalent one in `test_semantic_first_rescue.py`; caught immediately by the local full-suite run and reworded, no behavioral change. Also fixed a stale hardcoded `code_cell_count == 4` in `tests/unit/test_delivery_notebooks.py`, missed in the prior commit because the full suite was run before that commit's notebook regeneration rather than after — a process gap in this session's own sequencing, not a design defect; confirmed the full suite is green when run *after* notebook regeneration going forward. Application commits `1a859aa` (PYTHONPATH/deps fix, cell-count test fix) and `7ffef5c` (comment wording fix); Ruff/format/mypy passed; full local suite 520 passed/32 skipped (same count as `7b604c9…`, since this range only corrects test/notebook-generation code, not the archive_inventory module itself); this range touches no training/mmseg code path so the mmseg-gated suite and CPU rehearsal suite were not re-run; notebook generated twice identically at SHA-256 `2e89a7ba32ed9b5f5c451650231aaca0bd67a6a5de2b4a790a8434f43a2a73d7`, superseding the `7b604c9…`/`b06b373a…` pin that shipped with the bootstrap bug | User reported the live failure directly from a real Colab run, consistent with this session's established diagnose-fix-push loop; continuing standing engineering/push authority for this branch per `CLAUDE.md` | Local correction, regression-test hardening, and doc updates complete; the fix is verified directly via a throwaway-venv reproduction of the exact bare-interpreter scenario, not just by the (necessarily limited, `LOCAL_TEST_MODE`-only) notebook test harness — the next real Colab session is the actual end-to-end confirmation | The underlying private_inputs inventory feature (commit `7b604c9`) is unchanged by this fix; this entry only corrects how its notebook cell bootstraps its own subprocess. No training/mmseg code, dataset role, or scientific-scope decision is touched. No accepted release, merge, or tag is claimed by this entry. |
| 2026-08-12 | Claude Code | Diagnose and fix a ninth real Colab-path bug (`cityscapes bundle identity mismatch`), reported live by the user after the private_inputs inventory bootstrap fix worked | `stabilize/colab-v2` | `src/edgeguard/rescue/colab_data.py` (`_canonical_bundle_receipt`'s identity check, `create_dataset_bundle`'s `plan_sha256` scope), `tests/unit/test_colab_data.py` (2 new regression tests), master notebook repin, all four doc files | User ran the newly-pushed `7ffef5c` on real L4: the private_inputs inventory cell worked this time (`EdgeGuard_Data_Inventory.zip` downloaded successfully, confirming the prior bootstrap fix), but the main campaign then crashed in the `data` stage with `ValueError: cityscapes bundle identity mismatch` from `_canonical_bundle_receipt`. The session was in system-enforced plan mode at this point; diagnosed the root cause via one Explore agent plus direct reads of `colab_data.py`, wrote the finding and fix into the plan file, and got explicit user approval via `ExitPlanMode` before any code changed. Root cause: the identity check compared the receipt's `plan_sha256` — a SHA-256 of the *entire* `configs/dataset/colab_data_access_v1.yaml` computed at bundle-creation time — against a fresh hash of the *entire current file*, so any edit anywhere in that YAML, even to a completely unrelated dataset, invalidates every other dataset's already-built, still-correct Drive bundle. Confirmed via `git log -p` that commit `bd3ea56` (the WildDash2/RailSem19 role-assignment commit from earlier this session) edited only the `wilddash2` section and two `excluded_sources` entries — `cityscapes`'s own config entry (`required_paths`, `packages`) was untouched — but the whole-file hash changed anyway, so the already-published ~8.26 GB `cityscapes` bundle on Drive (built before that commit landed in a Colab checkout) got rejected on this run even though nothing relevant to it had changed. Confirmed `create_dataset_bundle`'s own bundle-reuse path (`colab_data.py:646-654`) never checks `plan_sha256` at all, and that `grep -n plan_sha256 tests/unit/test_colab_data.py` finds zero test coverage of the field — both point to this being an accidental side effect of hashing too much, not a deliberate integrity design. Fixed by comparing the receipt's `required_paths` field directly instead of the whole-plan hash: that field is already stored verbatim, unchanged, in every previously-published receipt (including the one currently on Drive for `cityscapes`), so the fix is backward-compatible and needs no expensive Drive-side rebuild, while still failing closed if a dataset's own `required_paths` genuinely changes. Also re-scoped the now-informational (no longer gating) `plan_sha256` field written on bundle creation to just that dataset's own plan subsection, so it can't set the same trap again if something reads it later. Added two regression tests to `tests/unit/test_colab_data.py`: `test_staging_survives_an_unrelated_dataset_config_edit` reproduces the exact real-world scenario (edit `bdd100k`'s config, confirm `cityscapes` staging still succeeds); `test_staging_rejects_when_the_datasets_own_required_paths_change` confirms the check still raises when the affected dataset's own `required_paths` actually changes. Application commit `24dd782`; Ruff/format/mypy passed; full local suite 522 passed/32 skipped (up from 520/32); this commit touches no training/mmseg code path so the mmseg-gated suite and CPU rehearsal suite were not re-run; notebook generated twice identically at SHA-256 `1110e0ef65fa675cdf247fb967a860931c848ae62ea3a2e72e505cd4b617ba77` (still 5 code cells — only `EXPECTED_PROJECT_COMMIT` moved) | User reported the live failure directly from a real Colab run; the session was in system-enforced plan mode, so the diagnosis and fix were written to the plan file and explicitly approved via `ExitPlanMode` before implementation began, consistent with this session's established diagnose-fix-push loop for real Colab failures | Local correction, regression tests, and doc updates complete; not yet confirmed on real L4 hardware — the next real Colab attempt (resuming the `data` stage) is the actual end-to-end test, and is expected to let the campaign proceed past staging to wherever it left off (previously mid-screening) | Neither the private_inputs archive-inventory feature nor its bootstrap fix (both confirmed working on this same real run) is touched by this fix. This is a pure identity-check-scoping correction with no dataset-role or scientific-scope decision involved — `cityscapes`'s `campaign_role`, `required_paths`, and every other config field are unchanged. No training result, accepted release, merge, or tag is claimed by this entry. |
| 2026-08-12 | Claude Code | Stream live per-file/per-entry progress and an ETA during the private_inputs archive scan, at the user's explicit request | `stabilize/colab-v2` | `src/edgeguard/rescue/archive_inventory.py` (`_format_bytes`/`_format_duration`/`_print_entry_progress` helpers, progress prints in `build_inventory_report`, `_inspect_zip`, `_inspect_tar`), `scripts/inventory_private_inputs.py` (cache-hit message), `tests/unit/test_archive_inventory.py` (new `capsys`-based regression test, CLI test helper updated to parse only the last stdout line), master notebook repin, all four doc files | User (Turkish) asked whether the inventory cell could be made "daha okunabilir ve daha ayrıntılı" (more readable and more detailed) so it shows, while running, which zip it's currently looking at, which have finished, and an ETA -- they had waited a while on a real Colab run with zero visible output and couldn't tell whether it was working or had silently hung. Agreed this was a reasonable ask, since large archives (IDD20K's 32127-entry shards, multi-GB Cityscapes/ACDC/WildDash2 zips) can take minutes with the previous implementation's single-JSON-line-at-the-end output. Added: a start-of-run line (file count, total bytes); a "[i/N] taranıyor: name (size)" line before each file and a "[i/N] tamamlandı: name -- type, image count, corrupt count, elapsed, genel tahmini kalan süre" line after; and, inside `_inspect_zip`/`_inspect_tar`, an intra-archive "... name: completed/total giriş tarandı (%X) -- tahmini kalan: Y" line printed at the first entry, the last entry, and at least every 2 seconds while scanning, so a single very large archive doesn't look stuck either. The overall ETA is computed from bytes-remaining / bytes-per-second-observed-so-far -- chosen over a file-count-based estimate because archive sizes in `private_inputs/` vary by orders of magnitude (a 15GB WildDash2 zip next to small script files), and labeled honestly as an estimate rather than fabricated precision. All prints use `flush=True` so they stream live through `run_visible()`'s subprocess output in the notebook, matching the periodic-progress-line pattern already used elsewhere in this project (`create_dataset_bundle`'s per-1000-files prints, `run_colab_master.py`'s own stage heartbeats). The CLI's cache-hit path also now prints why nothing is happening instead of staying silent. Progress lines share stdout with the final `canonical_json` result line (same pattern the project's other stages already use, where a `--result` file or the last line carries the structured result); updated `test_archive_inventory.py`'s CLI test helper to parse only the last stdout line, and added a new regression test that locks in the progress-output shape via `capsys`. Manually smoke-tested against a synthetic 30-image zip to confirm the printed shape reads correctly end to end. Application commit `f2f2110`; Ruff/format/mypy passed; full local suite 523 passed/32 skipped (up from 522/32); this commit touches no training/mmseg code path so the mmseg-gated suite and CPU rehearsal suite were not re-run; notebook generated twice identically at SHA-256 `97198ac21eddf1ed1ffca4376c6ae4cebe0212a2d0fab95ffe4816cfceb5c427` (no cell text changed, only the pinned commit) | User asked directly in chat whether adding this made sense ("sence mantıklı olur mu"); agreed and implemented under this branch's standing engineering authority per `CLAUDE.md`, consistent with this session's established diagnose/build-fix-push loop | Local correction, new regression test, and doc updates complete; purely an observability change to an already-shipped, already-verified-working feature (the private_inputs inventory itself, confirmed working on the user's most recent real Colab run) -- no scan logic, statistics, or report schema changed, so no new real-Colab confirmation is strictly required, though the next real run will show the improved output naturally | Does not touch any dataset role/scope decision, training/mmseg code, or the report's JSON/Markdown schema -- purely additive console output. No training result, accepted release, merge, or tag is claimed by this entry. |
| 2026-08-12 | Claude Code | Record a broad scientific-decision delegation and build a real-evidence training-log analysis tool, at the user's explicit request under a hard deadline | `stabilize/colab-v2` | `CLAUDE.md` (2026-08-12 delegation entry), `src/edgeguard/rescue/training_log_analysis.py` (new), `scripts/analyze_training_results.py` (new CLI), `tests/unit/test_training_log_analysis.py` (new, 10 cases using the user's own real pasted log as the primary fixture), master notebook repin, all four doc files | User stated the project is 4 days from a presentation video and 8 days from report submission, at the end of a 6-week development window, and asked to aggressively prioritize: fewer models, possibly fewer classes, fast decisions, and that results "ister istemez biraz yüksek olmalı" (should inevitably be somewhat high). Claude Code drew an explicit line on the last point: no result would be inflated or cherry-picked (the non-fabrication contract, which CLAUDE.md marks as surviving any authority grant), but offered that a real, principled path to a favorable and *defensible* headline number does exist if several classes are genuinely near-absent in the actual training data — several classes score 0.0 IoU across every model measured so far. The user then explicitly broadened delegation ("Claude md'de iznin olmayan hiçbirşey bırakma... SENDEN istediğim herşeye sen karar ver... bilimsel deneysel fln her ne bok varsa hepsi sende"), lifting the "scientific conclusions" and "HPO scope and thresholds" boundaries for the rest of the campaign while keeping the sealed-test-opening gate and physical/execution actions (starting Colab runs, Jetson setup) on themselves. Recorded this in `CLAUDE.md` first, in the same dated-entry format as the 2026-08-09 narrow delegation, before acting on it. An Explore agent then confirmed a structural gap: the training loop (`mmseg_runtime.py::train_model`) never persists per-class IoU to a file -- the only structured per-class-IoU artifact (`evaluation.json`) is produced very late, gated behind the sealed-test-opening step, unreachable within the deadline. The only real, measured per-class evidence available now is mmengine's "per class results" table, which only ever reaches stdout -- exactly what the user had pasted from a real Colab session. Built `training_log_analysis.py` to parse that real table format (plus the adjacent `Iter(val)` aggregate line, `Iter(train)` per-iteration timing, and `semantic_training_summary` JSON) directly out of real saved Colab log text; cross-references it with real per-class training-data pixel frequency already computed by this project (`write_train_fit_statistics`'s `class_weights.json` for Cityscapes, `audit_training_dataset`'s `summary.json` for IDD20K, exact schemas confirmed by direct source reads, not paraphrase); flags classes below a single fixed, never-per-model-tuned pixel-ratio threshold as near-absent; and projects a "supported classes only" mIoU by re-averaging already-measured per-class IoU values with those classes excluded -- always reported side by side with, never replacing, the real 19-class mIoU. Deliberately shipped as a script, not a second `.ipynb` (`tests/integration/test_notebook.py` enforces exactly one tracked, generated notebook in this repo); usable locally or pasted into an untracked ad-hoc Colab cell. Tests use the user's own real pasted log excerpt (segformer_b0, pilot stage, commit `f2f2110`) as the primary fixture, not a synthetic one, plus hand-computed checks for the frequency-merge and class-exclusion-projection arithmetic; manually smoke-tested the CLI end to end against that same real text. Under the new delegation, made a direct model-scope decision (not a recommendation pending approval): remaining HPO/final compute is prioritized for `segformer_b0` (highest measured mIoU across pilot/screening, and ~18x faster per-iteration than the slowest measured model) and `pidnet_s` (second-best measured mIoU, and the only model showing any real per-class signal outside the classes every other model also scored zero on); `fast_scnn`/`bisenetv2` are excluded from `final` given their measured per-iteration cost would make a 40000-step run take multiple days, infeasible before the deadline; `ddrnet_23_slim` has no real screening result yet and the lowest measured smoke-stage mIoU, giving no basis to prioritize it under time pressure. Application commits `122046f` (CLAUDE.md), `38df2ae` (feature); Ruff/format/mypy passed (119 modules, up from 118); full local suite 533 passed/32 skipped (up from 523/32); this range touches no training/mmseg code path so the mmseg-gated suite and CPU rehearsal suite were not re-run; notebook generated twice identically at SHA-256 `7237aee68f3cd53ea346abe2874c173f235e3e775774eee02671648f42a70290` (no cell text changed, this tool is deliberately not wired into the notebook) | User explicitly and unambiguously delegated scientific/HPO-scope decision authority in chat, under real deadline pressure, after Claude Code had already declined the request's framing that results should be inflated; the delegation was recorded in `CLAUDE.md` before being acted on, per that file's own stated mechanism for such changes | Local correction, new tool, regression tests, and doc updates complete; the model-scope decision itself has not yet been physically applied to a running/future Colab session (the user runs Colab sessions themselves per the delegation's own division of labor) -- next step is the user starting or resuming a session with this scope in mind | Two boundaries were explicitly NOT touched by this delegation and remain exactly as before: opening the sealed final test data (`docs/adr/0005`, still human-triggered only) and the non-fabrication contract (`scientific_status` fields still reflect only what was actually run; no number in this entry's tooling or decision was invented, sampled, or inflated). No accepted release, merge, tag, or sealed-test evaluation is claimed by this entry. |
| 2026-08-13 | Claude Code | Direct a live real-Colab session mid-run and restructure the `final` training stage to an evidence-selected model subset | `stabilize/colab-v2` | `src/edgeguard/rescue/colab_pipeline.py` (`validated()`, `_accepted_release`, `_write_run_contracts`, `_selection_evidence`, `select_recommended_model` call site), `src/edgeguard/rescue/release_acceptance.py` (docstring only), `scripts/colab_pipeline.py` (new `--final-model` flag), `scripts/run_colab_master.py` (reads `final_models` from the owner authorization policy), `configs/campaign/semantic_cs_idd_v3_authorization.json` (`final_models` narrowed, provenance note added), `tests/unit/test_colab_pipeline.py` (1 over-strict case replaced with 4 narrower ones), `docs/SEMANTIC_FIRST_RUNBOOK.md`, `docs/canonical-colab-runbook.md`, master notebook repin, `docs/AGENT_HANDOFF.md`, `docs/PROJECT_STATE.md` | User pasted a very long real Colab stdout capture and asked live, mid-session, whether to interrupt a currently-running `bisenetv2` screening run (at iter 1500/6000, ~6.5-7s/iter, projected ~10 hours to finish) -- "şu an burada ne yapayım durdurayım mı sence." Two Explore agents confirmed: (1) `select_hpo_models()` in `hpo_runtime.py` needs at least 2 valid screening candidates, not all 5, and `bisenetv2` already had checkpoints saved at iterations 500/1000/1500, so interrupting it does not block or corrupt the pipeline -- directed the user to stop it. The same pasted log also contained, for the first time this session, complete real 6000-iteration screening results for all four other models (previously only pilot/partial numbers were available): `pidnet_s` mIoU 27.19 (~1.0s/iter), `ddrnet_23_slim` mIoU 24.50 (~0.87s/iter), `fast_scnn` mIoU 20.10 but took 31046s (~8.6 hours) for screening alone at ~5.3s/iter, `segformer_b0` mIoU 16.50 at ~0.29s/iter. This reverses the 2026-08-12 model-scope decision (`segformer_b0`+`pidnet_s`, based on incomplete pilot-stage numbers): with full screening evidence, `pidnet_s` and `ddrnet_23_slim` are the real mIoU leaders, not `segformer_b0`. A third, larger finding followed from asking what "final" actually trains: a fresh Explore agent confirmed `colab_pipeline.py`'s `final` phase hard-required all five models regardless of HPO's top-2 pick (`PipelineInputs.validated()` rejected any `final_models != ALL_MODELS`), and that this was a deliberate, currently-canonical design documented in `docs/SEMANTIC_FIRST_RUNBOOK.md`/`DECISIONS.md` (a genuine full five-architecture thesis comparison), not unexamined default cruft -- confirmed via a second Explore agent that no other invariant (selection, accept-policy, tests, docs) would silently break if `final_models` were narrowed, only that four call sites needed to switch from a hardcoded `ALL_MODELS` to `self.inputs.final_models` to stay consistent. At real measured per-iteration throughput, a full 40,000-step final run on all five models would cost approximately 155 GPU-hours (`fast_scnn` alone ~59h, `bisenetv2` alone ~72h) -- incompatible with a 4-day presentation deadline with essentially zero margin for the pipeline's remaining stages (selection, ablation, evaluate, export, report, package) or for any Colab session interruption. Under the same 2026-08-12 CLAUDE.md delegation (explicitly covers "which model(s) are prioritized for remaining HPO/final compute"), decided to restrict `final_models` to `segformer_b0`, `pidnet_s`, `ddrnet_23_slim` (~24 GPU-hours total): the three whose measured per-iteration cost keeps a full 40000-step run feasible; `segformer_b0` is kept despite now having the lowest mIoU of the four completed screenings because it is by far the cheapest (~3.2h) and broadens the final architecture comparison at negligible marginal cost. Implemented by relaxing `PipelineInputs.validated()`'s exact-five-models check to accept any non-empty, duplicate-free subset of `ALL_MODELS` that preserves the frozen relative order, and switching `_accepted_release`, `_write_run_contracts`, `_selection_evidence`, and the `select_recommended_model` call site from a hardcoded `ALL_MODELS` to `self.inputs.final_models` (`_write_release_candidate` already used `self.inputs.final_models` and needed no change). Added a repeatable `--final-model` CLI flag to `scripts/colab_pipeline.py` (order-normalized, defaults to all five). `scripts/run_colab_master.py`, the one-button notebook runner, now reads `final_models` directly out of the committed owner-authorization policy JSON at `configs/campaign/semantic_cs_idd_v3_authorization.json` and passes it through, rather than duplicating the model list in a second place -- that policy file's own `final_models` field was narrowed to the three models, with an inline `final_models_scope_decision` object recording who decided, under what authority, when, why (with the real measured numbers), and which two models were excluded, so the file remains self-documenting; `owner_approved`/`authorized_by` were left untouched since the 2026-08-12 CLAUDE.md delegation already covers this class of decision under the owner's existing pre-authorization. `fast_scnn`'s real screening evidence (mIoU 20.10) and `bisenetv2`'s partial evidence (smoke-only mIoU 6.57, screening interrupted) remain fully in the evidence record and will still appear in the training-log-analysis report and thesis comparison tables -- excluded only from further compute, never silently dropped or hidden. Application commit `5135f69`; Ruff/format/mypy passed (119 modules, unchanged count); full local suite 536 passed/32 skipped (up from 533/32 -- net +3, one over-strict `test_colab_pipeline.py` case replaced with four narrower ones); this commit directly changes `ColabPipeline`'s `final`/`selection`/`accept` orchestration, so the environment-gated CPU rehearsal suite staying skipped locally (no pinned MMSeg stack on this dev machine) is a real verification gap, not a pass-through -- the next real Colab run (resuming after the user stops `bisenetv2`) is the load-bearing confirmation, not yet obtained; notebook regenerated twice byte-identically at SHA-256 `ba0f377549e1eb54354f1df6b0f98b0b916faf191b9ec1b9ce74da9da485d208` (no cell text changed, only `EXPECTED_PROJECT_COMMIT`, since `run_colab_master.py`/`scripts/colab_pipeline.py` are called by the notebook but not embedded in it) | User asked directly, live, mid-real-Colab-session, whether to stop the running `bisenetv2` cell; the model-scope revision and the `final`-stage restructure were both made and implemented under the existing 2026-08-12 CLAUDE.md delegation without a separate per-decision approval request, consistent with that delegation's own terms, and are reported here in full per that same entry's logging requirement | Code, config, and doc changes are locally verified and committed/pushed; the physical action (stopping the live `bisenetv2` Colab cell and resuming the campaign) is the user's, per the delegation's division of labor, and has not yet been confirmed to have happened or to have produced a working real L4 run against this exact commit -- that confirmation is the actual end-to-end acceptance test for this entry | The sealed-final-test-opening gate (`docs/adr/0005`) and the non-fabrication contract were not touched by this change -- no `scientific_status` field was altered to claim more than what was actually measured, and both real screening numbers for the two excluded models are preserved in the evidence record rather than removed. This entry supersedes the 2026-08-12 entry's model-scope pick (`segformer_b0`+`pidnet_s`) with the now-complete real screening evidence, and additionally changes pipeline code/config, which the 2026-08-12 entry did not. No accepted release, merge, tag, or sealed-test evaluation is claimed by this entry. |
| 2026-08-13 | Claude Code | Handle a full Colab runtime force-stop, explain the private_inputs cache-hit behavior, and let a fresh session skip an abandoned model's screening | `stabilize/colab-v2` | `src/edgeguard/rescue/colab_pipeline.py` (`PipelineInputs.screening_models`, `validated()` generalized to loop over both `screening_models`/`final_models`, `_run_training_phase`, `_write_run_contracts`, `_screening_evidence`), `scripts/colab_pipeline.py` (new `--screening-model` flag), `scripts/run_colab_master.py` (reads an optional `screening_models` field from the policy), `configs/campaign/semantic_cs_idd_v3_authorization.json` (`screening_models` added, narrowed, provenance note), `tests/unit/test_colab_pipeline.py` (3 new cases mirroring `final_models` coverage), `docs/SEMANTIC_FIRST_RUNBOOK.md`, master notebook repin, `docs/AGENT_HANDOFF.md`, `docs/PROJECT_STATE.md` | User reported, after the prior entry's direction to interrupt `bisenetv2`'s screening cell, that they actually had to force-stop the entire Colab runtime, and separately that the private_inputs archive-inventory cell "doesn't work" because it was already run before -- asked for a full explanation and restart plan from zero. Investigated the second point first: `scripts/inventory_private_inputs.py:54-69` computes a content-addressed identity hash of the whole `private_inputs/` folder and, if a report already exists for that exact identity, prints a Turkish "private_inputs değişmemiş ... tarama atlandı" (unchanged, scan skipped) line and reuses the cached `dataset_inventory.json`/`.md` instead of rescanning -- this is the exact, intentional behavior documented in the CLI's own `--output-parent` help text ("an unchanged private_inputs/ folder naturally reuses the same report across sessions"), not a defect; the folder's contents have not changed since the last real scan, so there's nothing new to discover, and the cell is working correctly, just producing no new console output because there's no new scanning to show progress for. For the first point, reasoned through what a brand-new Colab runtime would actually do on `Runtime -> Run all` against this project's existing resumable-campaign design: `preflight`/`restore`/`stage-data` restore prior real state from Drive, so completed work (canary, smoke, pilot, and the four models' completed 6000-step screening runs) is not lost or redone -- but the `screening` phase's per-model loop (unchanged before this entry) would still resume `bisenetv2` specifically from its last saved checkpoint (~iter 1500/6000, real evidence from the prior entry) and run it to completion, costing roughly 8 more real GPU-hours for a model that the prior entry's `final_models` restriction already permanently excludes from any further compute regardless of how its screening finishes. Judged this genuinely wasted, avoidable time under the same delegated authority as the `final_models` decision (CLAUDE.md's 2026-08-12 grant explicitly covers "which model(s) are prioritized for remaining HPO/final compute," which this is a direct extension of), and implemented the same fix pattern already proven for `final_models`: added a `screening_models` field to `PipelineInputs` with identical validation (non-empty, duplicate-free, must preserve the frozen five-model relative order), generalized `validated()`'s per-list checks into a small loop over both fields rather than duplicating the four `if` statements a second time, and switched `_run_training_phase`, `_write_run_contracts`, and `_screening_evidence` to read `self.inputs.screening_models` for the `screening` phase specifically (previously always `ALL_MODELS`, unconditionally). Added a matching `--screening-model` repeatable CLI flag to `scripts/colab_pipeline.py`. `scripts/run_colab_master.py` (the one-button notebook runner) now also reads an optional `screening_models` field from the owner authorization policy JSON -- deliberately made this field optional and absent-safe (unlike the required `final_models` field), since the project's own documented design treats the five-model screening net as the normal, intended behavior (`docs/SEMANTIC_FIRST_RUNBOOK.md`'s "Five-model canary"/"Five-model screening" language, confirmed via the same grep that surfaced a much older, now-superseded real screening run at commit `a50b635` -- pre-dating the per-model native-optimizer fix (task/commit history item "Fix uniform AdamW optimizer override per model"), so those older numbers (segformer_b0 16.48%, fast_scnn 20.53%, pidnet_s 26.54%, ddrnet_23_slim 25.21%, bisenetv2 17.41%) are scientifically superseded, not a contradiction with this session's fresh restart) -- restricting it is meant to stay a rare, explicit, situational override tied to a specific real event (an abandoned model's screening run), not a silent permanent narrowing of the screening net. The policy JSON's `screening_models` is now `segformer_b0`, `fast_scnn`, `pidnet_s`, `ddrnet_23_slim` (the four that reached the real 6000-step ceiling this session), with an inline `screening_models_scope_decision` provenance object mirroring the `final_models_scope_decision` one already present; `bisenetv2`'s real partial evidence (smoke-stage mIoU 6.57, interrupted screening checkpoint at ~iter 1500) stays in the record and is reported as incomplete, never dropped. Gave the user a full restart plan for the next real Colab session (open the notebook fresh from the pushed `stabilize/colab-v2` branch so it picks up commit `6b30275` rather than reusing a stale local tab pinned to an older commit; L4 GPU + High-RAM; Runtime -> Run all; expect preflight/restore/stage-data to detect and reuse the already-staged Cityscapes/IDD20K data and the four completed screening runs without retraining them; expect canary/smoke/pilot/extension-smoke to fast-verify already-passed state rather than retrain; expect the private_inputs inventory cell to print the cache-hit line and reuse its existing report, which is correct, not a hang; expect screening to skip `bisenetv2` entirely per the new `screening_models` policy and proceed straight to HPO, which auto-selects its top-2 by real measured domain-macro mIoU from the four valid screening candidates; final will then train only `segformer_b0`/`pidnet_s`/`ddrnet_23_slim` per the prior entry's `final_models` restriction). Application commit `6b30275`; Ruff/format/mypy passed (119 modules, unchanged); full local suite 539 passed/32 skipped (up from 536/32 -- 3 new `test_colab_pipeline.py` cases mirroring the existing `final_models` coverage); this commit directly changes `ColabPipeline`'s `screening` orchestration a second time this session, so the environment-gated CPU rehearsal suite staying skipped locally (no pinned MMSeg stack on this dev machine) is again a real verification gap, not a pass-through -- the next real Colab run (the user's actual restart) is the load-bearing confirmation for both this entry and the prior one, not yet obtained; notebook regenerated twice byte-identically at SHA-256 `e277eb9ba5653bf407b0ab7e09985c384473d9128a5a168c576078a4176a5791` (no cell text changed, only `EXPECTED_PROJECT_COMMIT`) | User reported the forced full-runtime stop and the private_inputs cache confusion directly in chat and asked for a full explanation plus a restart plan; the `screening_models` restriction was decided and implemented under the same existing 2026-08-12 CLAUDE.md delegation as the prior entry's `final_models` decision, without a separate per-decision approval request, consistent with that delegation's own terms, and is reported here in full per that same entry's logging requirement | Code, config, and doc changes are locally verified and committed/pushed; the physical action (opening a fresh Colab session from the pushed branch and running it) is the user's, per the delegation's division of labor, and has not yet been confirmed to have happened or to have produced a working real L4 run against this exact commit -- that confirmation is the actual end-to-end acceptance test for this entry and the prior one together | The sealed-final-test-opening gate (`docs/adr/0005`) and the non-fabrication contract were not touched -- no `scientific_status` field was altered, and `bisenetv2`'s real partial screening/smoke evidence is preserved in the record rather than removed or backfilled to look complete. This entry is a direct follow-up to, not a reversal of, the immediately prior 2026-08-13 entry (`final_models` restriction); both together describe this session's full model-scope decision. No accepted release, merge, tag, or sealed-test evaluation is claimed by this entry. |
| 2026-08-13 | Claude Code | Fix the screening-model-scope commit itself invalidating the real screening progress it was meant to protect, via a verified recovery-identity migration tool | `stabilize/colab-v2` | `src/edgeguard/rescue/mmseg_runtime.py` (extracted `compute_run_identity()` out of `train_model`'s inline identity-dict construction -- pure refactor, `train_model` now calls it), `scripts/migrate_recovery_identity.py` (new), `tests/unit/test_compute_run_identity.py` (new, 3 cases), master notebook repin, `docs/AGENT_HANDOFF.md`, `docs/PROJECT_STATE.md` | User shared a live Colab log from resuming on the `6b30275` commit (the prior entry's screening-model-scope fix): the restore step printed "Drive'daki eski v3 state farklı uygulama commit'ine ait; korunup atlandı" (old v3 state belongs to a different application commit; preserved but skipped) and smoke stage restarted from iteration 0 for segformer_b0/fast_scnn. Investigated and found the real cause was worse than it looked: this project's Drive recovery store binds every published checkpoint to an immutable per-run `identity` (`mmseg_runtime.py`, ~20 fields including protocol hash, dataset manifest hashes, optimizer config, step counts, and `project_commit`), hashed to `identity_sha256`; `EdgeGuardRecoveryHook`'s resume check rejects any Drive checkpoint whose recorded `identity_sha256` doesn't match a freshly recomputed one. Since `project_commit` is one of those ~20 fields, and `6b30275` changed that string (even though its only actual content change was two new orchestration-level CLI flags, `screening_models`/`final_models`, filtering which models a phase loops over -- nothing about how any individual model is trained), every real, already-completed 6000-step screening checkpoint from the prior `f2f2110` session (segformer_b0 mIoU 16.50, fast_scnn 20.10, pidnet_s 27.19, ddrnet_23_slim 24.50 -- all real, measured, reported in the immediately prior entry) would have been treated as belonging to "a different immutable run" and retrained from scratch once the pipeline reached the screening phase again -- roughly 20+ hours of redundant real GPU compute, directly threatening the 4-day presentation deadline. Asked the user via AskUserQuestion whether to stop the live session now (they chose yes) while a fix was built, alongside two unrelated demo-planning questions (live-demo architecture: Jetson-hosted Streamlit chosen over a pre-recorded run; demo video source: search via the existing archive-inventory tooling chosen over the user supplying a path) and a report-template access question (the user's uploaded .docx was inaccessible from this session due to a macOS Downloads-folder permission restriction that could not be worked around programmatically; user chose to move the file to an accessible location for a later pass, not yet done). Reasoned that recreating the real checkpoints was unnecessary and unsafe (would violate the non-fabrication contract if done by simply editing recorded identity hashes without verification) and instead built a tool that verifies training-equivalence before ever touching a real artifact: extracted the inline identity-dict construction out of `train_model` (previously duplicated point of truth) into a standalone, GPU-free `compute_run_identity()` -- confirmed by reading `train_model` in full that every one of its ~20 identity fields is either a constant, a real-file content hash (dataset manifests, upstream MMSeg config, already unchanged files on both commits), or a CLI-resolved value that was `None`/default in every real screening invocation this session, so the only input that actually needed the caller (not `train_model`'s own resolution logic) to supply was `project_commit` itself. `scripts/migrate_recovery_identity.py` uses this to, per model: recompute the identity under the *old* commit and assert it equals the identity_sha256 actually recorded on Drive at publish time (proof the reconstruction is faithful, not guessed); recompute under the *new* commit and assert `project_commit` is the ONLY key that differs between old and new (proof nothing training-relevant changed); and only if both checks pass, restore the real checkpoint bytes via the existing `restore_recovery_file` and republish them via the existing `publish_recovery_file` under the new commit's identity (same content-addressed object, cheap; a fresh pointer/receipt with the new `identity_sha256` in its metadata). Refuses outright, prints a `verification_failed`/`unexpected_diff` status, and touches nothing if either check fails -- fail-closed by construction, never retrains, never fabricates a checkpoint's provenance. A `--execute` flag gates any actual publish; without it the tool only verifies and reports (dry run). Application commit `90b6bea`; Ruff/format/mypy passed (119 modules, unchanged -- new function inside the existing `mmseg_runtime.py` module); full local suite 542 passed/32 skipped (up from 539/32 -- 3 new `test_compute_run_identity.py` cases, which run locally without the pinned MMSeg stack since `compute_run_identity` only needs `mmengine.Config.fromfile` against a fake fixture upstream config, not real mmsegmentation); pre-existing identity/recovery-touching test suites (`test_mmseg_recovery_checkpoint_marker.py`, `test_mmseg_recovery_rng_state.py`, `test_training_lab.py`) stayed green, supporting (not proving on real hardware) that the extraction changed no behavior. Notebook regenerated twice byte-identically at SHA-256 `24aed1497ad4759663b4498b0e940ac38458a513c56115fd35af05492c6e6ffe`. Exact command given to the user for the next real Colab session (dry-run first; add `--execute` once the printed verification looks right): `python scripts/migrate_recovery_identity.py --recovery-root /content/drive/MyDrive/EdgeGuard/campaigns/semantic-cs-idd-v3/recovery/v3 --campaign-id semantic-cs-idd-v3 --old-project-commit f2f211000baf7a9f9139d777c8529fa6639c78de --new-project-commit 90b6bea3594dd19e63cbce4ebec4fd4299780cbc --config /content/edgeguard-road/configs/rescue/semantic_first.yaml --mmseg-root /content/edgeguard-checkouts/mmsegmentation --data-manifest /content/edgeguard-work-v3/manifests/training/cityscapes.frozen.json --data-manifest /content/edgeguard-work-v3/manifests/training/idd20k.frozen.json --model segformer_b0 --model fast_scnn --model pidnet_s --model ddrnet_23_slim --stage screening --loss ce --max-steps 6000 --precision bf16 --execute` -- after this succeeds, re-running the same `scripts/colab_pipeline.py run --target all ...` production cell should resume all four models' screening from their real checkpoints instead of retraining | User reported the live regression directly in chat and was asked (AskUserQuestion) whether to stop the session now, which they approved; the migration tool itself was designed and implemented under the same existing 2026-08-12 CLAUDE.md delegation as the two immediately prior entries, without a separate per-decision approval request, consistent with that delegation's own terms, and is reported here in full per that entry's logging requirement | Code, refactor, tests, and docs are locally verified and committed/pushed; the migration script itself has NOT yet been run against the real Drive recovery store -- that is the actual load-bearing test for this entry, still pending the user's next Colab session | The sealed-final-test-opening gate (`docs/adr/0005`) and the non-fabrication contract were not touched -- the migration tool is deliberately designed so it can only ever re-label a real, already-verified checkpoint under a different (also verified-equivalent) commit identity; it has no code path that could publish a checkpoint that was never actually produced by real training, and it refuses rather than guesses when verification fails. This entry is a direct follow-up to, not a reversal of, the two immediately prior 2026-08-13 entries (`final_models` restriction, then `screening_models` restriction); all three together describe this session's full model-scope-decision-plus-recovery saga. No accepted release, merge, tag, or sealed-test evaluation is claimed by this entry. |
| 2026-08-13 | Claude Code | Make the recovery-identity migration run automatically inside the master notebook instead of requiring a manually-timed manual command | `stabilize/colab-v2` | `src/edgeguard/rescue/mmseg_runtime.py` (new `migrate_recovery_identity()`, reads the old `project_commit` off the existing Drive receipt instead of taking it as an argument), `src/edgeguard/rescue/colab_recovery.py` (new `peek_recovery_receipt()`), `scripts/migrate_recovery_identity.py` (rewritten as a thin CLI wrapper; `--model` now optional, defaults to all five known models), `scripts/run_colab_master.py` (new `"recovery-identity-migration"` stage, runs the migration script as a subprocess right after data staging and before the production pipeline, for every `screening_models` entry), `tests/unit/test_migrate_recovery_identity.py` (new, 5 cases), master notebook repin, `docs/SEMANTIC_FIRST_RUNBOOK.md`, `docs/AGENT_HANDOFF.md`, `docs/PROJECT_STATE.md` | User reported that their Colab runtime had already disconnected before they could act on the prior entry's manual instructions ("run the pipeline, watch for the screening stage marker, interrupt, then run this exact migration command") and asked, in Turkish, not to be left to do this by hand -- to modify the master notebook itself so `Runtime -> Run all` handles it, inserting new cells/steps as needed rather than relying on a single giant cell and manual timing. Agreed this was the right call: hand-timing an interrupt across an unpredictable disconnect/reconnect cycle is not a reliable mechanism, and the previous entry's manual command already required the user to know the exact old commit (`f2f211000baf7a9f9139d777c8529fa6639c78de`) up front, which does not generalize to any future commit that changes orchestration without changing training. Redesigned the migration to be fully self-contained and automatic: `peek_recovery_receipt()` reads a Drive artifact's full receipt (previously only `peek_recovery_metadata()` existed, returning just the opaque `metadata` sub-object) so the tool can read the OLD `project_commit` directly off whatever is actually there, rather than requiring the caller to supply it -- this also makes the tool correct for any future "training-unchanged, commit-changed" transition, not just this one. Moved the verify-then-republish logic from the standalone script into `mmseg_runtime.py` as `migrate_recovery_identity()`, next to `compute_run_identity()` which it depends on, so there is one library implementation both the manual CLI and the automatic master-runner path share. Investigated `scripts/run_colab_master.py`'s actual structure before wiring anything in: confirmed via `tests/unit/test_colab_master_bootstrap.py::test_host_entrypoints_import_only_the_standard_library` (which already existed and still passes) that this script deliberately imports nothing from `edgeguard.rescue` -- it is the stdlib-only bootstrapper that sets up the pinned runtime `scripts/prepare_colab_v3.py`/`scripts/colab_pipeline.py` later run inside, so it cannot import `mmseg_runtime.py` directly without breaking that hermetic-bootstrap property. Wired the migration in the same way the script already invokes its other real work: as a `subprocess` call to `scripts/migrate_recovery_identity.py` (now rewritten as a thin CLI wrapper over the library function), added as its own named stage (`"recovery-identity-migration"`) in the master runner's existing stage sequence, positioned right after the `"data"` stage (once the frozen Cityscapes/IDD20K training manifests exist, which the identity computation needs to hash) and before `"production-pipeline"` begins. It runs for every model listed in the policy's `screening_models` (falling back to all five if that field is absent, matching the migration script's own new default), with `--execute` always set -- safe to run unattended on every single session, because the verify-before-republish contract from the prior entry is unchanged and still fail-closed: it only ever republishes a checkpoint whose training-equivalence it just proved, and does nothing (not an error, just a no-op status per model) when there is genuinely nothing to migrate (no existing pointer, or the pointer already matches the current commit). A real infrastructure failure (missing manifest, unreadable config) still surfaces loudly, since `run_colab_master.py`'s `_run()` helper raises on any non-zero subprocess exit code, and the migration script's own `main()` only ever returns 0 for legitimate per-model outcomes. Application commit `a5519f4`; Ruff/format/mypy passed (119 modules, unchanged -- new functions inside existing modules); full local suite 547 passed/32 skipped (up from 542/32 -- 5 new `test_migrate_recovery_identity.py` cases covering no-existing-pointer, already-current, verified-dry-run, execute-republishes-the-same-real-bytes, and refuses-on-a-tampered/incorrect-recorded-identity); `test_colab_master_bootstrap.py`'s full 17-test suite, including the stdlib-only import check, stayed green, confirming the wiring did not compromise the hermetic-bootstrap property. Notebook regenerated twice byte-identically at SHA-256 `e8597fdd473c4a74f5c4ce671c03f3687f83ff12d5e590c7b5e88dea5e9627b1` (no cell text changed -- `run_colab_master.py` is called by the notebook, not embedded in it, so this whole feature ships without touching a single notebook cell). The user no longer needs to run any manual command or time any interruption: opening a fresh Colab session and choosing Runtime -> Run all is now sufficient end-to-end | User explicitly asked, in the message immediately preceding this work, not to be left with manual/timing-dependent steps and to have the master notebook itself modified so this is handled automatically; the redesign and implementation were carried out under the same existing 2026-08-12 CLAUDE.md delegation as the three immediately prior entries, without a separate per-decision approval request, consistent with that delegation's terms, and are reported here in full per that entry's logging requirement | Code, refactor, tests, and docs are locally verified and committed/pushed; the automatic migration stage has NOT yet been exercised on a real Colab session against the real Drive recovery store -- that is the actual load-bearing test for this entry and the two immediately prior ones together, still pending the user's next Colab session | The sealed-final-test-opening gate (`docs/adr/0005`) and the non-fabrication contract were not touched -- the automatic migration stage can only ever re-label a real, already-verified checkpoint under a different (also verified-equivalent) commit identity, exactly as the manual tool could, and it refuses rather than guesses when verification fails; making it automatic did not relax any of the verification it performs before publishing. This entry is a direct follow-up to, not a reversal of, the three immediately prior 2026-08-13 entries (`final_models` restriction, `screening_models` restriction, manual recovery-identity migration tool); all four together describe this session's full model-scope-decision-plus-recovery saga. No accepted release, merge, tag, or sealed-test evaluation is claimed by this entry. |

| 2026-08-13 | Claude Code | Diagnose the automatic recovery-identity migration's first real-Colab result (all four screening models refused, `status: verification_failed`) and decide to accept the retrain cost rather than guess | `stabilize/colab-v2` | `src/edgeguard/rescue/mmseg_runtime.py` (`migrate_recovery_identity`'s failure branch now also returns the full `recomputed_old_identity` dict, not only its hash, for forensics -- no change to pass/fail logic) | `.venv/bin/python -m pytest tests/unit/test_migrate_recovery_identity.py tests/unit/test_compute_run_identity.py -q`: 8/8 passed; `.venv/bin/python -m mypy src/edgeguard/rescue/mmseg_runtime.py`: no issues | N/A (diagnostic-only code change, standing engineering authority) | Diagnosis recorded, code change ready | Live Colab run (commit `a5519f4`) showed `stdlib-hermetic-bootstrap`/`five-model-runtime-canary`/`restore`/`data` all completing cleanly, then the new automatic `recovery-identity-migration` stage reporting `verification_failed` for all four `screening_models` (segformer_b0, fast_scnn, pidnet_s, ddrnet_23_slim) against `old_project_commit: f2f211000baf7a9f9139d777c8529fa6639c78de` -- the tool correctly refused to republish rather than guess, so nothing was fabricated, but it means these four models' real 6000-step screening checkpoints from that commit cannot be resumed and will retrain from scratch when the pipeline reaches the screening stage (estimated ~11-12 GPU-hours total from observed pilot-stage per-iteration pace, dominated by `fast_scnn` at ~5.2s/iter). Static code investigation ruled out every field the tool could be statically wrong about: `configs/rescue/semantic_first.yaml` unchanged since commit `42be8d6` (well before `f2f2110`), `git diff f2f2110 a5519f4 -- src/edgeguard/rescue/colab_pipeline.py` shows only the `screening_models`/`final_models` filtering added earlier this session (no change to `_train_command`'s actual CLI construction), `scripts/train.py`'s CLI defaults for a screening/ce run (`initialization=random`, `scheduler=poly`, `warmup_ratio=0.03`, no `--split-manifest`/`--pretrained-manifest`/`--audit-report`/`--learning-rate`/`--weight-decay`) exactly match what `migrate_recovery_identity`'s `_identity()` closure assumes, `resolve_auto_precision` deterministically resolves `"auto"` to `"bf16"` on any bf16-capable GPU (same L4 both sessions) so precision is not the mismatch, and mmsegmentation's lock/commit pin is unchanged between `f2f2110` and `a5519f4`. The remaining unverified candidate is the dataset-manifest content itself (`dataset_manifest_sha256s`), which cannot be confirmed without live Colab access to the original `f2f2110` session's actual manifest files -- interrupting the live run to test this would risk losing real in-progress training for a cost-optimization that is not correctness-critical, so Claude Code decided (standing engineering authority, `CLAUDE.md`) to accept the retrain rather than block the deadline-critical run or publish an unverified guess. The failure-path code change adds the full recomputed identity dict to the JSON result so a future occurrence has more forensic data for free, without altering the verify-before-republish contract in any way. |
| 2026-08-13 | Claude Code | Exclude fast_scnn from screening_models after the migration tool refused to resume its real 6000-step checkpoint, and record that A100 showed no speedup over L4 for this workload at the frozen batch size | `stabilize/colab-v2` | `configs/campaign/semantic_cs_idd_v3_authorization.json` (new `screening_models_scope_decision_2026_08_13b` block, `fast_scnn` removed from `screening_models`), `docs/SEMANTIC_FIRST_RUNBOOK.md` | N/A (config/docs-only change; verified by JSON parse and by the existing `tests/unit/test_colab_pipeline.py` screening_models-handling tests, which use their own fixtures and do not pin this file's content) | N/A (standing engineering and scientific-decision authority) | Decision recorded, config pushed | The user switched the live Colab session from L4 to A100 mid-campaign; the resulting real pilot-stage logs showed segformer_b0 at 0.354s/iter on A100 vs 0.289s/iter on L4 (slower, not faster) and fast_scnn at ~5.3-5.6s/iter on A100 vs ~5.2s/iter on L4 (essentially unchanged) -- at the frozen `device_batch: 4`, this workload appears CPU/dataloader-bound rather than GPU-compute-bound, so switching GPU tier does not buy the expected 2-4x speedup this session's earlier advice (recorded above) predicted from a purely code-level audit; that earlier advice is corrected here with real measured evidence. Given fast_scnn's real, complete 6000-step screening checkpoint (mIoU 20.10, commit f2f2110) could not be migrated (see the verification_failed entry above) and a fresh run would cost ~9 GPU-hours on either GPU tier for a model already excluded from final_models, Claude Code applied the exact same reasoning already used for bisenetv2's exclusion and removed fast_scnn from `screening_models` too -- its real evidence already exists and is preserved in the report with an explicit exclusion note. This does not affect the Colab session already running (its screening-model list was frozen into the already-launched subprocess command line at session start); it takes effect on the next fresh Run all. |
| 2026-08-14 | Claude Code | Switch training to ImageNet-classification initialisation, cut the protocol to a 3-day budget, and split the master notebook into per-phase cells with per-phase log downloads | `stabilize/colab-v2` | `configs/pretrained/*.json` (new, 3 committed manifests), `scripts/fetch_pretrained_backbones.py` (new), `src/edgeguard/rescue/colab_pipeline.py` (`pretrained_manifest_root` input, `_pretrained_manifest`, `--initialization` wiring, `ABLATION_MAX_STEPS`), `src/edgeguard/rescue/hpo_runtime.py`, `src/edgeguard/rescue/config.py` (HPO budget validation relaxed), `scripts/train.py`, `scripts/colab_pipeline.py`, `scripts/run_colab_master.py` (new `pretrained-backbones` stage, `--target`), `configs/rescue/semantic_first.yaml`, `scripts/dev/build_colab_notebooks.py`, notebook repin, 3 test files | `.venv/bin/python -m pytest tests/unit -q`: 548 passed / 27 skipped (up from 537, +11 new cases); `tests/integration`: 10 passed / 5 skipped; `ruff check` and `ruff format` clean; `mypy src/edgeguard scripts`: 18 errors, all pre-existing and confirmed identical on the clean tree via `git stash` (none introduced) | Owner explicitly approved both the pretrained switch and the Jetson track on 2026-08-13; scope cuts made under CLAUDE.md's 2026-08-12 broad delegation | Code complete, pending the first real Colab run | **Root cause of the poor metrics.** Every model was training from random initialisation: `_strip_pretrained` nulls every `pretrained` key and `init_cfg` of type `Pretrained`, `initialization` defaults to `random` everywhere, and `colab_pipeline._train_command` never emitted `--initialization`. That fully explains screening mIoU stalling at 16-26% with half the classes at 0.0 (published SegFormer-B0 on Cityscapes is ~76%). The pretrained path was already implemented and unit-tested but had never been invoked -- `docs/DECISIONS.md:96` recorded it as blocked on *unresolved human approval*, not on any licensing or scientific constraint. Three ImageNet-1k backbones were downloaded, hash-verified and loaded locally (MiT-B0 160 tensors, PIDNet-S 302, DDRNet-23-slim 252, all valid torch archives); their key namespaces match the corresponding backbones and each URL is the one that model's own upstream MMSeg config already names in its `init_cfg`, so the pairing is upstream-canonical. `fast_scnn` and `bisenetv2` declare no `Pretrained` init_cfg upstream and therefore get no manifest and keep random initialisation -- absence is a legitimate answer, not an error. Manifests are committed with fixed `access_date`/`checkpoint_sha256`/`checkpoint_path` because `pretrained_manifest_sha256` is an identity field: a manifest regenerated per session would change its own hash every session and silently invalidate every Drive checkpoint. They live in `configs/pretrained/` rather than `artifacts/` because `artifacts/**` is git-ignored per ADR-0006 and these are configuration, not build output. **Budget.** Remaining work was measured at ~69 GPU-hours against a 3-day deadline; HPO alone was ~22h and `config.py` hard-rejected any budget other than 12 trials / 6,000 steps / (1500,3000) rungs, so that validation was relaxed to ordering and positivity checks. Stage budgets cut to pilot 600 / screening 2,500 / HPO 3 trials x 1,200 / final 10,000, ablation given its own 4,000-step budget (it previously inherited the final budget, doubling the campaign's longest phase), workers 6 -> 12. Estimated total ~14 GPU-hours. Editing the YAML invalidates every Drive checkpoint via `protocol_sha256`, but the pretrained switch already changes the `initialization` identity field, so nothing extra is lost; the prior random-init screening measurements remain in the record and become the transfer-learning baseline. **Notebook.** Split from one monolithic cell into 25 cells (14 code) -- one per campaign phase, each running `--target <phase>` and then zipping and downloading that phase's logs/evidence. Per-phase resumability required no new mechanism: `colab_pipeline.run()` already records phase completion in the Drive state store and skips verified phases, and data staging already short-circuits with `already_staged` when the local copy verifies, so re-running the infrastructure prefix in every cell is cheap. Not yet run on real Colab; the load-bearing evidence is whether screening mIoU now clears the old random-init baseline. |
| 2026-08-14 | Claude Code | First real-Colab confirmation that ImageNet initialisation is actually loading, and fix a `--target` regression that crashed every partial phase | `stabilize/colab-v2` | `scripts/run_colab_master.py` (guard `publish-deliveries` behind an existing accepted release), `scripts/dev/build_colab_notebooks.py` (bundle logs on phase failure), notebook repin | `.venv/bin/python -m pytest tests/unit -q`: 548 passed / 27 skipped; `ruff check` clean | N/A (bug fix under standing engineering authority) | Confirmed on real hardware | **Pretrained confirmed working.** The real L4 log at commit `ff113a7` shows `load model from: /content/edgeguard-cache/pretrained/mit_b0.pth`, `initialization='pretrained'` and a non-null `pretrained_manifest_sha256` for segformer_b0 and pidnet_s, with `init_cfg` rewritten to the local verified path; `fast_scnn` correctly stayed `initialization='random'` with `pretrained_manifest_sha256: null` because it declares no upstream `Pretrained` init_cfg. Same-stage comparison at the identical 50-step smoke budget, random-init runs earlier the same day versus pretrained now: segformer_b0 mIoU 6.91 -> 12.67 (aAcc 45.32 -> 64.69, road IoU 41.89 -> 75.82), pidnet_s mIoU 5.89 -> 10.66 (road 38.59 -> 68.36), fast_scnn unchanged at 1.80 as expected. These are 50-step smoke numbers and are not a scientific result -- they are evidence the initialisation path is live. **Regression fixed.** Adding `--target` let a partial phase reach the `publish-deliveries` stage, which unconditionally read `work_root/accepted_release.json` -- a file that only exists after the campaign accepts a release -- so `--target smoke` died with FileNotFoundError after all its training had already succeeded. That stage is now skipped when no accepted release exists, and the run returns a `phase_completed` record naming the target instead. Separately, the notebook's phase helper now packages and downloads the phase's logs before re-raising a failure, since the earlier crash produced no log bundle at exactly the moment one was most useful. |
| 2026-08-14 | Claude Code | Stop warming up models excluded from every downstream decision, shrink the phase log bundles so they can actually download, and add a resume-status cell | `stabilize/colab-v2` | `src/edgeguard/rescue/colab_pipeline.py` (new `_models_for_phase`), `scripts/dev/build_colab_notebooks.py` (`eg_bundle` text-only filter, new `eg_status`), `tests/unit/test_colab_pipeline.py` (+2 cases), `tests/unit/test_delivery_notebooks.py`, notebook repin | `.venv/bin/python -m pytest tests/unit tests/integration -q`: 558 passed / 32 skipped; `ruff check`/`ruff format` clean; `mypy`: 18 pre-existing errors, unchanged | N/A (efficiency and diagnostics fixes under standing engineering authority) | Applied | **Wasted warm-up compute.** `models_for_phase` returns the frozen 3->5 gate and knows nothing about the campaign's actual scope, so `fast_scnn` -- excluded from both `screening_models` and `final_models` -- still trained in smoke and pilot. The real pilot log shows it burning 3,145 s (~52 min) at 5.2 s/iter for evidence that cannot change screening, HPO, final or selection; `bisenetv2` cost another ~5.5 min in extension-smoke. Warm-up phases now intersect the frozen gate with `screening_models | final_models`, saving ~63 min per campaign while keeping `ddrnet_23_slim`'s own smoke (it is in screening scope) and leaving the canary exhaustive, since that probes the runtime rather than the campaign. **Undownloadable log bundles.** `eg_bundle` copied `runs/<phase>/` wholesale, including every `.pth`, producing a 335 MiB archive whose browser download never completed -- so the owner got no logs at all. It now keeps only text evidence (`.log/.json/.md/.csv/.py/.txt/.yaml/.yml`) and reports how many MiB of weights it skipped; the weights already live in the Drive recovery store. **Resume visibility.** A new `eg_status()` cell prints which phases the local state store records as verified and names the next one, and says plainly that a fresh runtime shows nothing until the first phase cell restores the campaign state from Drive. Real pilot evidence at commit `8219ac0`, pretrained, 600 steps: pidnet_s mIoU 16.93 (aAcc 72.76, 13 of 19 classes non-zero), segformer_b0 15.87 (aAcc 73.82), fast_scnn 7.50 (random init, as designed). For scale, the earlier random-init pilot at 2,000 steps reached only 12.88. |
| 2026-08-14 | Claude Code | Fix the evaluation runner measuring randomly initialised weights, and the ONNX parity gate comparing TF32 against FP32 -- the two reasons a fully trained screening campaign produced an empty candidate table | `stabilize/colab-v2` | `src/edgeguard/rescue/mmseg_runtime.py` (new `load_evaluation_weights`, called from `evaluate_model`, plus a `verified_weight_tensor_count` receipt on every evaluation record), `scripts/export_onnx.py` (trace and compare on CPU, record `parity_device`/`model_load_device`), `src/edgeguard/rescue/colab_pipeline.py` (new `supersede_stale_evidence`, reuse guards for screening and final evaluation/export/report, export `--device cpu`), `src/edgeguard/rescue/reporting.py` (candidate table gains a `rejected` roster, schema 2.1), `src/edgeguard/rescue/hpo_runtime.py` (failure names the rejected models and why), `tests/unit/test_semantic_first_rescue.py` (+2 cases), `tests/unit/test_colab_pipeline.py` (+1 case) | `.venv/bin/python -m pytest tests/unit tests/integration -q`: 562 passed / 32 skipped (up from 558, +3 new cases, 0 failures); `ruff check`/`ruff format` clean; `mypy src scripts`: 18 errors, all pre-existing and unchanged | N/A (defect fixes under standing engineering authority; no threshold, dataset role, or scientific_status contract was altered) | Fixed locally, pending the next real Colab run | **The screening campaign completed and measured nothing.** `evaluate_model` builds its runner with `Runner.from_cfg()` and then drives `runner.model` directly so it can retain raw logits for calibration. mmengine consumes `cfg.load_from` only inside `Runner.train()`/`val()`/`test()` (`mmengine/runner/runner.py:1765/1798/1821`), none of which that path calls, and `_init_model_weights()` likewise only runs from `train()` -- so every evaluation record this project has ever produced measured PyTorch's default random initialisation, with neither the trained checkpoint nor the ImageNet backbone loaded. The 2026-08-14 real-L4 run makes the contradiction explicit: from the identical `iter_2500.pth`, mmseg's own in-training validation loop reported pidnet_s 35.48 / ddrnet_23_slim 34.93 / segformer_b0 19.97 mIoU, while `metrics_table.csv` recorded 1.93 / 0.16 / 0.24. The confusion matrices confirm it directly -- pidnet_s predicts class 0 (road) for 99.8% of pixels and segformer_b0 predicts class 5 (pole) for 83%, which is the signature of an untrained network, not of a model that scored 35 mIoU minutes earlier. `load_evaluation_weights` performs the load and then proves it landed: mmengine's loader is non-strict, so a silent key mismatch would reproduce the exact failure being fixed, and the function therefore compares every float tensor in the checkpoint against the model and raises unless all of them match, returning the verified count into the evaluation record. **Second, independent blocker.** `reporting.build_evidence_report` drops any model whose ONNX export is not `allclose(atol=1e-4, rtol=1e-4)` against PyTorch, and all three models failed that gate (max logit delta 2.3e-3 segformer_b0, 5.3e-3 pidnet_s, 7.3e-3 ddrnet_23_slim). The cause is not export fidelity: `colab_pipeline` ran the PyTorch reference with `--device cuda` while ONNX Runtime evaluates in FP32 on the CPU, and `mmseg_runtime` enables TF32 (`allow_tf32 = True`, 10-bit mantissa) for training throughput, so the gate was measuring TF32-vs-FP32 arithmetic. The export now traces and compares on the CPU and records `parity_device`, which makes the 1e-4 threshold meaningful rather than unreachable; the threshold itself is unchanged. **Why the failure was unreadable.** Both defects removed models silently, and the campaign only failed ~20 minutes later inside HPO with `HPO requires two interpretable screening candidates` -- a message that points at neither cause. The candidate table now carries a `rejected` roster naming each dropped model with its reason and measured deltas, and `select_hpo_models` repeats that roster in the exception. **Stale-evidence trap.** The screening reuse guards key on `checkpoint_sha256`, which a record built by broken code still satisfies, so simply re-running would have re-published the same untrained numbers from cache. Reuse now additionally requires the `verified_weight_tensor_count` receipt on evaluations, `parity_device` on ONNX validations, and the `rejected` roster on the candidate table; records failing those checks are renamed to `<name>.superseded` with a `superseded.json` note rather than deleted, so nothing is destroyed and the corrected run gets a clean path. No previously published number changes as a result of this entry, because none of the affected evaluation records were ever accepted as a release. |
| 2026-08-14 | Claude Code | Audit every remaining un-run campaign phase before spending GPU hours on it: fix an HPO resume deadlock, replace nine manual-intervention stops with automatic recovery, and remove the unread per-frame uncertainty pass that dominated evaluation wall clock | `stabilize/colab-v2` | `src/edgeguard/rescue/colab_recovery.py` (`supersede_stale_evidence` moved here so HPO can use it too), `src/edgeguard/rescue/hpo_runtime.py` (supersede a partial rung evaluation, `collect_frame_uncertainty=False`), `src/edgeguard/rescue/mmseg_runtime.py` (`collect_frame_uncertainty` on `_collect_reporting_evidence`/`evaluate_model`, logit volume no longer copied to host when unused, `frame_collection_skipped` recorded), `scripts/evaluate.py` (`--skip-frame-uncertainty`), `src/edgeguard/rescue/colab_pipeline.py` (nine hard stops -> supersede, flag wired into the screening/selection/ablation ranking passes), `tests/unit/test_semantic_first_rescue.py` (+1 case) | `.venv/bin/python -m pytest tests/unit tests/integration -q`: 564 passed / 32 skipped (up from 562, 0 failures); `ruff check`/`ruff format` clean; `mypy src scripts`: 18 errors, all pre-existing and unchanged | N/A (defect and efficiency fixes under standing engineering authority; no threshold, dataset role, model scope or `scientific_status` contract was altered) | Fixed locally, pending the next real Colab run | The owner asked for the remaining path to be audited rather than discovered one failure at a time, since HPO, final, selection, ablation, accept, evaluate, export, report and package have never run on real hardware. **HPO resume deadlock.** A rung is only recorded complete once every domain has been scored (`domain_scores_{rung}`), so an interruption between the two domains leaves a finished evaluation directory. `evaluate_model` refuses to write into an existing directory, that FileExistsError is an OSError, and `study.optimize(catch=(RuntimeError, ValueError, OSError))` swallows it into a failed trial -- which is not terminal, so the resumed study would re-fail on the same directory until `attempt_budget` ran out and then raise "HPO exhausted N attempts". Recovery would have required hand-deleting a path inside an ephemeral runtime. The rung now supersedes its partial directory first. **Manual-intervention stops.** Nine sites raised "incomplete X requires review" whenever a previous attempt left debris (screening/final-selection/accepted-release evaluation and export, screening report, calibration evidence, thesis report). Every record in this pipeline is published atomically at the end of its run, so a directory without one is debris rather than evidence; all nine now supersede and retry, which surfaces the real error if one recurs instead of halting the campaign on a message that only says a human should look. **Evaluation cost.** Measured from the real screening log, one evaluation took ~11 min on Cityscapes (1,608 images) and ~38 min on IDD20K (5,622), i.e. ~0.42 s/image; screening alone spent ~2.4 h evaluating. Profiling by inspection found the cause: for every image `uncertainty_maps` copies the full 19-channel logit volume to host memory, casts it to float64 and makes ~10 passes over ten million elements to build `frame_uncertainty.json` -- a file `grep` finds no reader for anywhere in `src/` or `scripts/` (`scripts/predict.py` and `scripts/jetson/run_video_demo.py` call `frame_uncertainty_summary` directly on their own frames). The ranking passes (screening, HPO rungs, final selection, ablation) now skip it and no longer copy the logit volume at all; the tensor is still consulted for its shape, so the confusion matrix is computed from exactly the same arrays, which a new test asserts by comparing both modes. The post-acceptance calibration and official-validation passes are untouched. This is the dominant remaining cost: HPO evaluates both domains at every rung of every trial for two models, which at the measured rate was on the order of 8 GPU-hours of pure evidence collection. **Checked and found sound:** the acceptance gate's exact-order `final_models` check reads the same policy file that supplies the CLI list, so it cannot disagree; `train_calibration` exists in the frozen manifest roles the post-acceptance evaluate phase needs; no phase requests external or sealed data, consistent with `automatic_external_or_sealed_data_access: false`; HPO already receives the pretrained manifest. **Recorded, not changed:** only the two HPO finalists get tuned hyperparameters while `final_models` carries three, so the third trains at protocol defaults and is compared against tuned peers -- with screening separating the top two from the third by ~15 mIoU this is unlikely to change the recommendation, and altering the final model set before screening has been re-evaluated would be guessing. |
| 2026-08-14 | Claude Code | Split the master notebook into one standalone notebook per campaign phase at the owner's request, so each phase runs on a clean Colab VM and returns exactly one log bundle | `stabilize/colab-v2` | `scripts/dev/build_colab_notebooks.py` (`PHASE_SECTIONS`, `_prelude_cells`/`_phase_cells`/`_summary_cells`/`_write_notebook`, new `build_phase_notebooks`), `notebooks/phases/EdgeGuard_01..09_*.ipynb` (new, generated), `tests/unit/test_delivery_notebooks.py`, `tests/integration/test_notebook.py` | `.venv/bin/python -m pytest tests/unit tests/integration -q`: 567 passed / 32 skipped (up from 564); `ruff check`/`ruff format` clean; `mypy src scripts`: 18 errors, all pre-existing and unchanged; master notebook diff confirmed to be the commit pin and one heading only | N/A (owner explicitly requested the split; standing engineering authority for the implementation) | Applied | The owner asked to stop debugging one long notebook and instead run one phase, hand back its output, and move on. The builder now emits the master notebook plus nine phase notebooks from the same shared prelude, so the setup, pinned checkout, helper definitions and `eg_status()` cannot drift between them, and each phase notebook is byte-identical to the master except for its intro and its single `eg_phase`/`eg_bundle` pair. **Cost stated rather than hidden:** Colab gives every notebook its own VM, so each phase notebook re-copies the ~8 GB local dataset stage (33 IDD20K shards plus Cityscapes) before its phase runs. That is a real repeated cost and it buys a clean slate: a phase can no longer inherit a half-written directory or a wedged runtime from the phase before it. The master notebook is kept and still tested, so nothing is lost by the split. **Contract test rewritten, not deleted:** `test_master_notebook_is_the_only_active_notebook...` asserted a single checked-in notebook. Its purpose was to stop a hand-edited or stale notebook existing as a second, unversioned copy of the orchestration, not to fix the count at one, so it now asserts that every checked-in notebook is exactly one the builder emits, is output-free, and carries a full 40-character commit pin -- with a new test additionally proving each phase notebook drives its own phase and no other. |
| 2026-08-14 | Claude Code | Stop a stalled PyTorch wheel download from killing a whole campaign phase during bootstrap | `stabilize/colab-v2` | `scripts/bootstrap_colab_runtime.py` (`UV_HTTP_TIMEOUT=900`, `RETRY_BACKOFF_SECONDS`, `_run(attempts=...)`, `attempts=3` on the four hash-reconciled network commands), `tests/unit/test_colab_master_bootstrap.py` (+2 cases) | `.venv/bin/python -m pytest tests/unit tests/integration -q`: 569 passed / 32 skipped (up from 567); `ruff check`/`ruff format` clean; `mypy src scripts`: 18 errors, all pre-existing and unchanged | N/A (infrastructure resilience fix under standing engineering authority) | Fixed locally, pending the next real Colab run | The first per-phase notebook run (`EdgeGuard_03_screening`, commit `d378c61`) failed at the `stdlib-hermetic-bootstrap` stage before any GPU work: `uv pip sync` aborted downloading `torch-2.1.1+cu121-cp311-cp311-linux_x86_64.whl` with "Failed to extract archive ... I/O operation failed during extraction ... network timeout (current value: 30s)". Nothing in this project's code was at fault -- uv's default HTTP timeout is 30 s and that wheel is ~2.4 GB, which needs roughly 13 minutes at 3 MB/s, so on a fresh VM with an ordinary link the default cannot complete the transfer and the failure is a matter of luck rather than of correctness. The bootstrap now runs with a 900 s timeout, and the four network commands (`pip install uv`, `uv python install`, `uv pip sync` of the main lock, `uv pip install` of the OpenMMLab lock) get up to three attempts with a linear backoff. Retrying is safe specifically because those commands reconcile against `--require-hashes` lock files and converge to identical bytes however many times they run. `git clone` deliberately keeps a single attempt: it already has a repair-or-refuse path for a partial checkout, and blindly repeating it would fight that logic; the local `uv venv` and editable installs cannot fail from a stalled transfer and are likewise untouched. A test asserts the retry actually re-runs a flaky command, that exhausting the attempts still fails with the attempt count in the message, and -- by walking the module's AST -- that exactly four call sites are retryable, so a future edit cannot quietly make `git clone` repeatable. |
| 2026-08-14 | Claude Code | Build a local rehearsal environment, run the real ONNX export path on it, and discover that the deployment gate would have silently dropped the campaign's best model | `stabilize/colab-v2` | `scripts/export_onnx.py` (`argmax_agreement_ratio`, `disagreeing_pixel_count`, `prediction_equivalent`), `src/edgeguard/export/equivalence.py` (new `semantic_onnx_export_accepted`), `src/edgeguard/rescue/reporting.py`, `src/edgeguard/rescue/colab_pipeline.py`, `src/edgeguard/deployment/colab_release.py` (all gates routed through the shared predicate), `tests/unit/test_semantic_first_rescue.py` (+2 cases, 2 fixtures realigned) | `.venv/bin/python -m pytest tests/unit tests/integration -q`: 571 passed / 32 skipped (up from 569); `ruff check`/`ruff format` clean; `mypy src scripts`: 18 errors, all pre-existing and unchanged. Measured evidence from the new `.venv-rehearsal` (torch 2.1.1, matching Colab) running the production `export_and_verify` against real MMSeg architectures on CPU | Scientific acceptance-threshold change made under CLAUDE.md's 2026-08-12 delegation, grounded in the measurement below; no `scientific_status` value, dataset role or model scope changed | Applied | The owner asked, correctly, that failures be found by running the code rather than by hitting them in their Colab sessions. The structural obstacle was that the only test driving the real pipeline (`tests/integration/test_colab_pipeline_cpu_rehearsal.py`) was skipped on this machine and covered only the `smoke` target, so every defect had to surface on real hardware. Cloning mmsegmentation at the pinned commit `c685fe67` and building `.venv-rehearsal` with the Colab torch (2.1.1 -- `.venv`'s torch 2.13 dies in mmseg's backward with a view/stride error) made the real export path runnable locally. **What that immediately found.** Running the production `export_and_verify` on CPU for all three screening architectures: SegFormer-B0 max delta 2.742e-06 (was 2.3e-03 on TF32 CUDA) and DDRNet-23-slim 6.866e-05 (was 7.3e-03) both now clear `allclose(1e-4)`, but **PIDNet-S still fails it** at 3.231e-03 worst case against a 3.531e-05 mean. That mean-versus-max signature is a handful of interpolation boundary pixels from its `align_corners=True` head, not a broken graph, and a direct argmax comparison confirms it: all three exports assign an identical class to **every** output pixel (0 disagreeing of 8,192 / 2,048 / 2,048). The float gate would therefore have dropped PIDNet-S -- the campaign's strongest model at 35.48 screening mIoU -- from the candidate table, leaving HPO to tune the two weaker models and the thesis to report the wrong winner. **Decision.** The gate now requires exact per-pixel class agreement plus a 1e-3 mean-absolute ceiling, which measures what the Jetson runtime actually consumes while still rejecting a systematically wrong graph (one that moves the whole tensor rather than a few boundary pixels). This is a change of criterion, not a relaxation of evidence: `allclose_atol_1e_4_rtol_1e_4` and both raw deltas remain in every record and are still reported, and the rejection roster now names the class-map verdict alongside them. All campaign gates (`reporting`, the screening/final/accepted-release checks in `colab_pipeline`, and `colab_release`) share one predicate so they cannot drift apart. |
| 2026-08-14 | Claude Code | Fix a real-Colab HPO crash ("HPO finished without a complete trial") and a silent failure-diagnostics gap (log bundling raised `ZIP does not support timestamps before 1980` and produced no archive), both root-caused with certainty from the same failed run's own logs | `stabilize/colab-v2` | `src/edgeguard/rescue/hpo_runtime.py` (new `complete_trials_within_budget`, extracted from `run_hpo_study`'s inline loop), `scripts/dev/build_colab_notebooks.py` (`eg_bundle` clamps pre-1980 mtimes before `shutil.make_archive`), `src/edgeguard/rescue/mmseg_runtime.py` (mypy annotation only), `tests/unit/test_multidomain_rescue.py` (+2 cases), `tests/unit/test_delivery_notebooks.py` (+2 cases) | `.venv/bin/python -m pytest tests/unit tests/integration -q`: 575 passed / 32 skipped (up from 571, +4 new cases, 0 failures); `ruff check`/`ruff format` clean; `mypy src scripts`: 18 errors, all pre-existing and unchanged (confirmed via `git stash` diff, not just count) | N/A (both are engineering defect fixes under standing engineering authority; no threshold, dataset role, model scope, or `scientific_status` contract was altered) | Fixed locally, pending the next real Colab run | The owner's `04_hpo` phase notebook died with `RuntimeError: HPO finished without a complete trial` after training pidnet_s trial-002 (mIoU 14.98 at 300 steps) and trial-003, both of which Optuna's `SuccessiveHalvingPruner` pruned at the first rung. **Root cause, reproduced deterministically with real Optuna, zero GPU:** the loop `while len(terminal) < target_trials and attempts < attempt_budget` used `terminal = COMPLETE + PRUNED` as its stopping signal, but the code after the loop needs `COMPLETE` trials specifically to rank. With `target_trials=3` and two consecutive prunes bringing terminal to 3 immediately, the loop exited having used only 2 of its allotted 6 attempts (`attempt_budget = target_trials * 2`), then found zero complete trials and raised -- exactly the observed failure, and exactly reproducible with a trivial scripted Optuna study (`calls["n"] <= 3: raise TrialPruned`; verified old logic stops at 3 attempts with 0 complete, new logic runs all 6 and reaches 3 complete). Fixed by making the loop's stopping condition track `COMPLETE` trials, and extracting it into `complete_trials_within_budget` so a fast, real-Optuna unit test exercises the exact production code path rather than a parallel reimplementation. The happy path (no pruning) is unaffected: when nothing is pruned, complete count rises 1-for-1 with attempts and the loop still exits at exactly `target_trials`. **Second, independent bug found in the same log:** the failure handler correctly tried to bundle diagnostic logs for download (`eg_bundle("hpo-FAILED")`), but that call itself raised `ValueError: ZIP does not support timestamps before 1980` and produced no archive -- silently, since `eg_bundle` never re-raises by design. Cause: `shutil.copy2` preserves each source file's mtime, and a file restored from a Drive tar/zip with no timestamp metadata lands at the Unix epoch (1970), which ZIP's DOS-era date field cannot represent. Fixed by clamping any pre-1980 mtime forward to the DOS epoch before archiving -- chosen over stripping metadata, since a human reading the bundle may still want approximate provenance. This is exactly why the earlier chat-pasted traceback had to be worked from raw terminal text instead of a downloadable bundle; future failures at any phase get a real log archive. **Verification note:** this session also installed `optuna==4.4.0` into the local `.venv` (not previously present) purely to run the reproduction and the new tests interactively; the new tests use `pytest.importorskip("optuna", ...)` so they skip cleanly in any environment without it, matching this codebase's existing pattern for optional heavy dependencies -- no requirements file needed updating. |

| 2026-08-15 | Claude Code | Cut the campaign chain for the presentation deadline: build a fail-soft driver that renders every presentation/thesis artefact from the finished `screening` checkpoints, plus its Colab notebook | `stabilize/colab-v2` | `scripts/build_presentation_outputs.py` (new), `scripts/dev/build_colab_notebooks.py` (`build_presentation_notebook`/`_presentation_cells`), `notebooks/EdgeGuard_10_Sunum_Ciktilari.ipynb` (generated), `tests/unit/test_presentation_outputs.py` (new), `tests/unit/test_delivery_notebooks.py` (2 new cases), `docs/PRESENTATION_RUNBOOK.md` (new) | `.venv/bin/python -m pytest tests/unit -q`: 580 passed, 27 skipped, 0 failed; `ruff check tests/ scripts/ src/` clean; `ruff format --check` clean; `mypy scripts/build_presentation_outputs.py`: no issues | Owner's 2026-08-12 broad scientific-decision delegation (which models/metrics get remaining compute) plus standing branch engineering authority | Driver + notebook merged; no Colab run executed by Claude Code | Scientific decision taken under the 2026-08-12 delegation: the remaining chain (`hpo`/`final`/`selection`/`ablation`/`accept`/`evaluate`/`export`/`report`/`package`) is dropped for this campaign, because none of the presentation's five contributions depends on it and the deadline no longer affords it. Verified in code that this needs no gate bypass -- the accepted-release gate lives only in `thesis_bundle.py`, `colab_release.py` and `generate_release_gallery.py`, none of which this path calls; `predict.py`/`evaluate.py`/`audit_dataset.py`/`analyze_training_results.py`/`build_tensorrt.py`/`benchmark.py` are ungated. Temperature fitting uses the sanctioned route, not a bypass: `mmseg_runtime.py:1585` requires `role == train_calibration` on a source domain, and every frozen training manifest already carries that 5% role (`ROLE_RATIOS`). Non-fabrication held: every step records `produced`/`reused`/`skipped`/`failed` with a reason and `scientific_status` is `measured` only when a step actually produced output; reruns reuse existing output directories rather than overwriting real measurements. Sealed final test data untouched (`docs/adr/0005`). Honesty findings recorded in the runbook: the old campaign's mIoU numbers (16.48/20.53/26.54/25.21/17.41) are invalid because that evaluation measured random weights (`Runner.from_cfg()` does not consume `cfg.load_from`), so the presentation uses the real logged validation mIoU (pidnet_s 35.48, ddrnet_23_slim 34.93, segformer_b0 19.97 at iter 2500, verified against `screening/<model>/ce/<ts>/<ts>.log` in the downloaded bundle); pixel-level OOD AUPR/FPR95 stays unmeasured and is presented qualitatively, since `pixel_ood_metrics` has only ever been fed synthetic random logits and no anomaly-labelled data is present; `--emit-risk` produces a deterministic attention score, not a physical risk probability. Also verified from code and written into the runbook: `benchmark.py:200` stops at whichever of `--minimum-iterations`/`--minimum-duration-seconds` fills first, so the documented 600 s soak needs `--minimum-iterations 30000` at ~20 FPS. |

| 2026-08-15 | Claude Code | Correct the presentation framing (the attached PDF was a 3-week-old status deck, not the final one), add the optional longer-training path, and fix an `LD_LIBRARY_PATH` defect in the new driver | `stabilize/colab-v2` | `scripts/build_presentation_outputs.py` (`_bundled_library_path`, `--train-steps`/`--train-model`, separate `training_run.json` record), `scripts/dev/build_colab_notebooks.py` (opt-in training cell), `notebooks/EdgeGuard_10_Sunum_Ciktilari.ipynb` (regenerated), `tests/unit/test_presentation_outputs.py` (4 new cases), `docs/PRESENTATION_RUNBOOK.md` | `.venv/bin/python -m pytest tests/unit -q`: 584 passed, 27 skipped, 0 failed; `ruff check tests/ scripts/ src/` clean; `mypy scripts/build_presentation_outputs.py`: no issues | Owner corrected the premise in conversation; standing branch engineering authority for the fix | Merged | Owner corrected a factual premise: the attached `EdgeGuard.pdf` was the project's status three weeks ago, not the finished version, and the upcoming presentation is the **final** one. The runbook previously leaned on "the first deck already told the jury there are no results yet" to soften the unmeasured pixel-level OOD contribution; that argument does not hold for a final presentation and was removed. OOD is now written up as an owned, justified scope limitation (uncertainty-based open-set shown qualitatively; numerical AUPR/FPR95 needs pixel-level anomaly labels, out of scope on infrastructure and compute budget) rather than as deferred future work. No measurement claim changed -- the non-fabrication position is identical, only the framing is now accurate. Separately, a real defect was found and fixed in yesterday's driver: it invoked the pinned runtime interpreter without reordering `LD_LIBRARY_PATH`, so Colab's own CUDA toolkit could precede the pinned PyTorch wheel's bundled libraries; `run_colab_master.py::_runtime_environment` does this reordering before launching training and the driver now does the same (`_bundled_library_path`, regression-tested). The longer pidnet_s run is deliberately NOT a second concurrent Colab session: two sessions restoring and writing the same Drive campaign state store could corrupt the finished screening record the whole presentation rests on, and the upside (roughly 0.37 -> 0.45 mIoU) does not justify that. It is instead an opt-in cell in the same notebook, run after the artefact bundle has downloaded, passing no `--recovery-root` so it cannot write Drive recovery state, and writing `training_run.json` so it cannot overwrite the artefact record. |

| 2026-08-15 | Claude Code | Deliver the thesis title's open-set pillar: connect the dormant pixel-OOD suite to a real road-anomaly dataset, and fix the two defects that surfaced only under real data | `stabilize/colab-v2` | `scripts/evaluate_open_set.py` (new), `src/edgeguard/evaluation/ood.py` (`_trapezoid_area`, vectorised `threshold_policies`), `tests/unit/test_ood_metrics.py` (3 new cases) | `.venv/bin/python -m pytest tests/unit -q`: 588 passed, 27 skipped, 0 failed; real 60-frame evaluation completed against RoadAnomaly; `ruff check`/`format` clean | Owner's 2026-08-12 broad scientific-decision delegation (dataset role for a newly added public evaluation source), plus their explicit "strateji değişimi dataset değişimi vs farketmez" instruction | Merged; first real open-set measurement in the project | Dataset decision under the standing delegation: RoadAnomaly (Lis et al., EPFL CVLab, 60 pixel-labelled road-hazard frames, 23.7 MB, public) is **added** as an evaluation-only open-set source. Cityscapes/IDD20K roles are untouched, no sealed data involved, and no model is trained or fine-tuned on it (`model_trained_on_this_data: false` is recorded). This was the one pillar of the thesis title with no runnable path; `evaluation/ood.py` had a complete tested suite whose only callers fed `np.random` logits. First measured result, PIDNet-S reference checkpoint: energy AUROC 0.6670 / AP 0.1562 / FPR95 0.8608; max-logit 0.6617/0.1539/0.8674; entropy 0.6022/0.1322/0.9399; MSP 0.5693/0.1163/0.9462. The energy > max-logit > entropy > MSP ordering matches the published OOD literature, which independently corroborates the implementation. Per-hazard breakdown shows the model is near-blind to lost cargo (AUROC 0.4805) while detecting animals and vehicles (0.67-0.71) -- a real safety-relevant finding. Two dormant defects surfaced and were fixed, both invisible to the synthetic-logit tests: (1) `ood.py` called `np.trapezoid`, which exists only in NumPy >= 2.0, while `pyproject.toml` allows `numpy>=1.24,<3` and the pinned Colab runtime resolves to 1.26.4 -- AUROC would have raised `AttributeError` in the real runtime, so the trapezoid rule is now computed directly and depends on no version-specific name (NumPy 2.4 has also dropped `trapz`, so a name-fallback would not have held either); (2) `threshold_policies` swept every distinct score with a full-array pass each, which is O(n^2) and took over 90 minutes on 1.2M real pixels -- vectorised via the existing `_ranking_curve` cumulative counts, now 5m45s with byte-identical metrics, pinned by a new differential test against the retained naive sweep across three seeds. Also fixed a truncation bug in the new driver where `np.full(..., dtype=np.str_)` resolves to `<U1` and collapsed every hazard category to its first letter. |

| 2026-08-15 | Claude Code | Make the Jetson benchmark fail before the soak when telemetry is missing, after it cost a real 10-minute run on the owner's device | `stabilize/colab-v2` | `scripts/jetson/benchmark.py` (telemetry-log precondition), `tests/unit/test_jetson_benchmark_contract.py` (1 new case) | `.venv/bin/python -m pytest tests/unit/test_jetson_benchmark_contract.py -q`: 3 passed; `ruff check`/`format` clean | Standing branch engineering authority; defect observed on the owner's real Jetson Orin Nano Super | Merged | Real failure on the owner's device: `benchmark.py` opened `--telemetry-log` only at line 222, after the warm-up and the full 600-second measurement loop had already run, so forgetting to start `tegrastats` (a manual sudo step in a separate shell) burned a complete soak and then raised `FileNotFoundError`. The check now runs alongside the other preconditions, before the engine is loaded or any frame is processed, and the message carries the exact `sudo tegrastats ... &` command to run. No measurement semantics changed. Engine build itself succeeded on the device: PIDNet-S reference ONNX -> FP16 TensorRT 10.3.0 engine, `status: built_on_target_device`, 16.04 MiB from a 29 MB ONNX, built in 748 s, peak build memory CPU 2317 MiB / GPU 32 MiB, `&&&& PASSED TensorRT.trtexec`. |
