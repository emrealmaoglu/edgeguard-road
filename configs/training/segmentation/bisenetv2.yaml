schema_version: "1.0"
record_type: semantic_model_config
experiment_id: EGX-SEG-STACK-BISEV2
model_family: bisenetv2
architecture: EncoderDecoder
backbone: BiSeNetV2
decode_head: FCNHead
auxiliary_heads: [FCNHead, FCNHead, FCNHead, FCNHead]
mmseg_config_relative_path: configs/bisenetv2/bisenetv2_fcn_4xb4-160k_cityscapes-1024x1024.py
num_classes: 19
ignore_index: 255
normalization: ImageNet RGB mean/std
baseline_crop: [512, 1024]
initialization:
  stack_probe: random
  project_training: random
  source:
    status: not_applicable
  notes: Random initialization is the baseline; any pretrained alternative needs a new source decision.
logits:
  output_kind: native_logits
  direct_pre_softmax: true
  layout: NCHW
  dtype: float32
  class_count: 19
  native_resolution: model_defined_record_at_runtime
  alignment_mode: bilinear
  alignment_target: edgeguard_common_grid
  align_corners: false
export_risk_notes: Bilateral branches and fusion operations need early export probing.
expected_training_memory_notes: Lightweight-to-medium candidate; measure branch activations in Colab.
