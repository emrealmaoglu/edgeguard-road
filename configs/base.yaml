# Common schema example for future non-smoke pipelines.
# WP-01 performs no inheritance, include, merge, or config composition.
schema_version: "1.0"
project_name: edgeguard-road
contract_version: "1.0"
record_schema_version: "1.0"
